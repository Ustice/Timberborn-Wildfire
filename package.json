{
  "scripts": {
    "blueprints:check": "bun scripts/generate-wildfire-fuel-blueprints.ts --check",
    "blueprints:generate": "bun scripts/generate-wildfire-fuel-blueprints.ts",
    "kanban:audit": "bun scripts/audit-kanban.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  }
}
