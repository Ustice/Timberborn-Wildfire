---
ticket: TWF-076
agent_level: High
role: worker
requires_qa: true
doc_only: false
dependencies:
  - TWF-075
  - TWF-114
write_scope:
  - src/Wildfire.Timberborn/**
  - tests/Wildfire.Core.Tests/**
  - docs/TEST_PLAN.md
  - kanban/all-tickets/TWF-076-apply-crop-burn-consequences.md
---

# TWF-076: Apply Crop Burn Consequences

## Goal

Use burn damage to reduce crop and harvestable yield as fuel burns, then kill and visually mark the crop when its yield is consumed.

## Why

The design says fire consumes burnable world value. Crops are the farming-facing version of that rule: as fuel burns away, harvest yield should fall, and a fully burned crop should die rather than remaining harvestable.

## Requirements

- Consume compact fire deltas through the Timberborn consequence path.
- Reduce crop and harvestable yields according to accepted burn damage.
- Kill fully burned crops when their remaining yield is consumed.
- Switch burned crops to an accepted dead, burned, or charred presentation when Timberborn exposes a safe visual path.
- Preserve host boundaries; do not move fire-spread rules into Timberborn code.
- Add deterministic tests for crop yield loss, full-burn death, duplicate-cell suppression, and no-op behavior for non-burnable or unmapped targets.
- Expose bounded QA/status telemetry for considered crop targets, yield lost, killed targets, and visual-state updates.
- Document live QA expectations in `docs/TEST_PLAN.md`.

## Dependencies

- `TWF-075` provides the burn damage state foundation.
- `TWF-114` provides resource fuel and flammability classification for yield-derived fuel value.

## Role

- Worker.
- Follow [worker.md](../roles/worker.md).

## Implementation Notes

- Start with harvestable/crop spec lookup and the burn damage target resolution from `TWF-075`.
- Damage capacity should be based on remaining crop or harvestable yield, adjusted by the `TWF-114` resource catalog when the harvested good is known.
- Safe no-op cases must include unmapped cells, unknown harvest resources, non-burnable yields, and unavailable plant-death or visual APIs.
- Do not emit one player alert per crop; publish aggregate counters for `TWF-080`.
- Expected counters include considered crops, burnable crops, yield lost, killed crops, visual updates, skipped unknown resources, and skipped unsafe APIs.

## Verification

- Run `git diff --check`.
- Run `dotnet test`.
- Run `dotnet build Wildfire.slnx`.
- QA must capture live evidence for at least one burned crop consequence, plus copied `Player.log` and status counters.

## Notes

- Tree burn consequences belong to `TWF-084`.
- Structure burn consequences belong to `TWF-077`.
- Avoid one alert per crop. Player-facing aggregation belongs to `TWF-080`.
- Relevant design reference: `docs/DESIGN.md` section 20, "Burn Damage State".
- 2026-05-03 coordinator: moved to `03-in-progress` for Sprint 7 in `~/repos/wildfire-TWF-076` on branch `codex/TWF-076-crop-burn-consequences`, based on reviewed and integrated `TWF-075` commit `00eac0e7ac12b1f7084be3e5bfaba2dbff1033e3`.
- 2026-05-03 coordinator: serialize this broad `src/Wildfire.Timberborn/**` and `tests/Wildfire.Core.Tests/**` implementation lane ahead of `TWF-084` tree consequences unless scopes are narrowed. Required review is a hard gate; if review fails, return through `03-in-progress` for fixes, then back to `04-verify` for fresh review before integration.
- 2026-05-03 worker result: deterministic implementation committed in `~/repos/wildfire-TWF-076` at `1b5083e719e1fd7d4220cda47fce0ea745d2a2b9`. The worker added an adapter-local crop/harvestable consequence sink downstream of `TimberbornBurnDamageService`, runtime aggregate crop-burn counters, and safe default unavailable API behavior.
- 2026-05-03 worker evidence: deterministic tests cover crop yield loss, full-burn death/visual requests through an injected safe API, duplicate-cell suppression, non-burnable no-op, unmapped targets, unknown harvest resources, skipped unsafe API behavior, and QA/status telemetry. Runtime defaults to an unavailable safe API and does not claim live yield loss, death, or visual updates without a real safe binding.
- 2026-05-03 worker verification: `git diff --check` passed, full `dotnet test` passed with `166` tests, and `dotnet build Wildfire.slnx` passed with `0` warnings and `0` errors.
- 2026-05-03 worker blocker: live Timberborn launch/load remains blocked, so no live evidence was captured or claimed. Before integration, QA still needs `Player.log`, command output, nonzero crop-burn counters, safe crop/yield/death or precise skipped-unsafe-API evidence, and proof there is no per-crop alert spam.
- 2026-05-03 coordinator: moved to `04-verify` for review. Even if review passes, do not move to `05-integration` until required live QA evidence passes.
- 2026-05-03 review: passed deterministic review on commit `1b5083e719e1fd7d4220cda47fce0ea745d2a2b9` with no blocking findings. Review confirmed `Wildfire.Core` remains unchanged, the implementation stays crop/harvestable-only, unavailable live Timberborn APIs fail safely through skipped telemetry, and tree, structure, storage, explosive, ash, beaver, release-setting, and per-crop alert behavior remain out of scope.
- 2026-05-03 review verification: `git diff --check 00eac0e7...1b5083e` passed, targeted `dotnet test --filter FullyQualifiedName~TimberbornCropBurnConsequenceTests` passed with `7/7`, full `dotnet test` passed with `166/166`, and `dotnet build Wildfire.slnx` passed with `0` warnings and `0` errors.
- 2026-05-03 coordinator: moved to `07-blocked`, not integration. Required live gate: deploy after Timberborn launch/load is unblocked, register at least one real crop or harvestable with known yield, capture compact-delta burn sequence, preserve `Player.log`, command output, and `qa-readiness`/`status` tokens showing nonzero crop-burn considered targets, show real yield loss/death/visual update through a safe API or precise `skipped_unsafe_apis` evidence, and prove no per-crop alert spam.
- 2026-05-06 live QA failed from branch `codex/TWF-076-crop-burn-consequences` at `1b5083e719e1fd7d4220cda47fce0ea745d2a2b9` under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-076-live-20260506T145228Z/`. The loaded save had a Kohlrabi at `x=25 y=25 z=3`, but the branch `qa-delta-stimulus` queued `x=25 y=25 z=11`; dispatch ran, but `crop_burn_considered_targets=0` and all crop consequence counters stayed zero. Moved back to `03-in-progress`; add a crop/harvestable targeting fix or depth-aware live stimulus before rerunning the crop gate.
- 2026-05-06 worker fix on `~/repos/wildfire-TWF-076-qa` branch `codex/TWF-076-crop-burn-consequences`: `qa-delta-stimulus` now prefers the nearest registered crop/organic harvestable burn-damage owned cell, including its real `z`, and falls back to the old center cell only when no crop/harvestable target is registered. Added regression coverage for the failed `50x50x23`, `25,25,3` Kohlrabi-depth shape. Verification passed: `git diff --check`, targeted QA command bridge tests, targeted crop burn consequence tests, `dotnet build Wildfire.slnx --no-restore`, and full `dotnet test --no-restore` with `167/167`.
- 2026-05-06 live QA rerun failed under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-076-live-rerun-20260506T150851Z/`. Deployed the branch fix, but `qa-delta-stimulus` still targeted `x=25 y=25 z=11`, crop counters stayed zero, and `crop_burn_unmapped_targets=1`; forcing the earlier save bytes did not make Timberborn load the expected stable autosave. Moved back to `03-in-progress`; prove live crop/harvestable target registration or add enough registration telemetry to explain why no registered crop target is present before rerunning.
- 2026-05-06 worker diagnostic fix on `~/repos/wildfire-TWF-076-qa`: the QA delta stimulus now consumes the burn-damage registry, prefers registered crop/organic harvestable cells when present, and reports `target_source`, `burn_damage_registered_targets`, and `burn_damage_registered_crop_burn_targets` so QA can distinguish "save has no registered crop target" from "stimulus targeted fallback." Verification passed on the branch; fresh review accepted the diagnostic fix for live QA.
- 2026-05-06 live diagnostic rerun failed under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-076-live-diagnostic-rerun-20260506T160457Z/` before the crop registration question could be answered. Timberborn did not command-responsively load the exact `QA Tunnels and Booms` save, and the branch `qa-delta-stimulus` still only queues one max-heat/max-fuel change with no evidence that heat is held for Jason's required `12` cycles. Moved back to `03-in-progress`; add sustained 12-cycle heat proof semantics and an exact-save/loadable proof path before rerunning.
- 2026-05-06 worker narrow fix on `~/repos/wildfire-TWF-076-qa`: `qa-delta-stimulus` now schedules a `12` dispatch sustained max-fuel/max-heat `SetCell` hold against the selected registered crop/harvestable target, and `status`/`qa-readiness` expose durable `qa_delta_stimulus_sustained_heat_*` proof tokens including requested cycles, completed cycles, remaining cycles, queued cycle, target coordinates/source, and last completed tick. `scripts/invoke-timberborn-command.ts` now supports `--require-sustained-heat-cycles 12` so QA can mechanically fail the gate if the hold did not complete.
- 2026-05-06 worker narrow-fix verification: `dotnet test --filter TimberbornQaCommandBridgeTests` passed with `31/31`, `dotnet build` passed with `0` warnings and `0` errors, `bun install --frozen-lockfile` and `bun run typecheck` passed, `bun scripts/invoke-timberborn-command.ts --help` showed the new sustained-heat option, and `git diff --check` passed. No crop alert path was added or changed, and `docs/world-consequence-first-pass.md` was not touched.
- 2026-05-06 coordinator note: code is ready for review, but autonomous live QA still needs an exact-save path. Either QA must manually load `QA Tunnels and Booms` before invoking the branch command, or a separate exact-save loader/confirmation path must be added before claiming an unattended crop live gate.
- 2026-05-06 review passed the narrow `12` cycle sustained-heat fix with no blocking findings. Reviewer reran `dotnet test --filter TimberbornQaCommandBridgeTests` with `31/31` passed, `dotnet build Wildfire.slnx --no-restore` with `0` warnings and `0` errors, `bun scripts/invoke-timberborn-command.ts --help`, and `git diff --check HEAD`. Keep in `04-verify` for live QA; do not move to `05-integration` until QA proves the exact intended crop/harvestable save loaded, the selected target came from a registered crop/harvestable rather than fallback, `qa_delta_stimulus_sustained_heat_completed_cycles>=12`, and crop-burn consequence counters are nonzero or precise safe-unavailable telemetry is captured with copied `Player.log` and command transcripts.
- 2026-05-06 live QA failed under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-076-live-qa-tunnels-booms-12cycle-20260506T165706Z/`. The branch deployed cleanly and exact save load was unambiguous: `Player.log` showed `QA Tunnels and Booms - 2026-05-06 12h57m, Day 1-11.autosave` from `~/Documents/Timberborn/ExperimentalSaves/QA Tunnels and Booms/2026-05-06 12h57m, Day 1-11.autosave.timber`. `caffeinate -disu` PID `6268` was active, the command bridge was responsive, and the `12` cycle sustained heat proof completed.
- 2026-05-06 live QA failure detail: `burn_damage_registered_targets=0` and `burn_damage_registered_crop_burn_targets=0`, so `qa-delta-stimulus` fell back to `qa_delta_stimulus_target_source=center_fallback` at `x=25 y=25 z=11`. The final proof showed `qa_delta_stimulus_sustained_heat_completed_cycles=12`, but all crop counters stayed zero: considered targets, burnable targets, yield lost, killed crops, visual state updates, and skipped unsafe APIs. This is now a live crop/harvestable registration failure for the exact save, not an exact-save or sustained-heat tooling failure. Move back to `03-in-progress`; fix live crop/harvestable registration before rerunning the gate.
- 2026-05-06 Jason QA direction: the next live QA pass should select a crop by hand in the loaded save rather than relying on the script's automatic crop target discovery. The implementation lane should support a manually selected known crop proof path if a command or telemetry surface is needed, while preserving the `12` cycle sustained heat proof and crop consequence counters.
- 2026-05-06 worker manual-selection support on `~/repos/wildfire-TWF-076-qa` branch `codex/TWF-076-crop-burn-consequences`: added live known-yield crop/harvestable registration plus selected-crop targeting. QA can manually select a crop or harvestable in Timberborn, then run the existing `qa-delta-stimulus`; when the selected object resolves to a registered crop/harvestable target, the command targets that cell and reports `qa_delta_stimulus_target_source=selected_crop_target`.
- 2026-05-06 worker manual-selection changed files: `src/Wildfire.Timberborn/TimberbornLiveCropBurnDamageTargets.cs`, `src/Wildfire.Timberborn/TimberbornFireRuntimeInitializer.cs`, `src/Wildfire.Timberborn/TimberbornFireRuntime.cs`, `src/Wildfire.Timberborn/TimberbornFireSystem.cs`, `src/Wildfire.Timberborn/Wildfire.Timberborn.csproj`, `tests/Wildfire.Core.Tests/TimberbornBurnDamageStateTests.cs`, and `tests/Wildfire.Core.Tests/TimberbornQaCommandBridgeTests.cs`. The worker did not touch `docs/world-consequence-first-pass.md`.
- 2026-05-06 worker manual-selection verification passed: `git diff --check`, `dotnet build Wildfire.slnx --no-restore`, focused `TimberbornBurnDamageStateTests` plus `TimberbornQaCommandBridgeTests` with `43/43`, full `dotnet test --no-build` with `170/170`, and `bun run typecheck`.
- 2026-05-06 coordinator: moved to `04-verify` for focused review of the manual selected-crop support. If review passes, live QA must deploy the branch, load the exact `QA Tunnels and Booms` save, select a known crop such as Kohlrabi by hand in Timberborn, run `qa-delta-stimulus`, wait for sustained heat proof, and preserve `status`, `qa-readiness`, and `Player.log`. Acceptance requires nonzero registered crop targets, `qa_delta_stimulus_target_source=selected_crop_target`, `qa_delta_stimulus_sustained_heat_completed_cycles>=12`, and nonzero crop consequence counters or precise safe-unavailable telemetry.
- 2026-05-06 review passed the manual selected-crop support with no blocking findings. Reviewer confirmed the selected-object path is adapter-side only, reports `selected_crop_target`, preserves the `12` cycle sustained heat proof, and has targeted crop-registration and command-bridge coverage. Verification passed: `git diff --check HEAD`, untracked-file whitespace check for `TimberbornLiveCropBurnDamageTargets.cs`, focused tests with `43/43`, `dotnet build Wildfire.slnx --no-restore`, `bun scripts/invoke-timberborn-command.ts --help`, `bun run typecheck`, and full `dotnet test --no-build` with `170/170`. Keep in `04-verify` for live manual-selection QA; do not move to integration until the exact manual selected-crop proof passes.
- 2026-05-06 manual-selection QA attempt under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-076-manual-crop-qa-20260506T171922Z/`: deploy and load succeeded, baseline `qa-readiness` was command-responsive, and the loaded map visibly contained crop/harvestable fields. The first QA agent could not produce a selected crop proof; the final screenshot showed a manually selected `Double Dynamite`, not a crop, so no accepted `qa-delta-stimulus` crop gate was run. A replacement QA attempt is scoped to deselect the dynamite, manually select an actual crop/harvestable, and fail fast if the selected panel cannot prove a crop target.
- 2026-05-06 manual-selection QA failed under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-076-manual-crop-continuation-20260506T173753Z/`. QA manually selected a visible harvestable `Blueberry` and preserved screenshot proof at `screenshots/03-selected-blueberry-before-stimulus.png`, then ran `bun scripts/invoke-timberborn-command.ts qa-delta-stimulus --wait=30 --require-advanced-tick --require-sustained-heat-cycles 12`. The command failed with `Selected_Timberborn_object_did_not_resolve_to_a_registered_crop_or_harvestable_burn-damage_target.`
- 2026-05-06 manual-selection QA failure detail: follow-up `status` and `qa-readiness` stayed command-responsive at `50x50x23`, but `burn_damage_registered_targets=0`, `burn_damage_registered_crop_burn_targets=0`, `qa_delta_stimulus_target_source=placeholder`, and all crop consequence counters remained zero. No accepted `12` cycle burn ran. This is now a selected harvestable registration/resolution bug, not a save-content, exact-load, or manual-selection blocker. Move back to `03-in-progress`; fix live selected crop/harvestable registration so a selected `Blueberry` resolves to `selected_crop_target`.
- 2026-05-06 worker selected-Blueberry fix on `~/repos/wildfire-TWF-076-qa`: live registration now resolves yield from direct `Yielder`, `Gatherable.Yielder`, or `Cuttable.Yielder`, so harvestable natural resources such as Blueberry can register as crop/harvestable burn-damage targets. The selected crop/harvestable resolution path is now testable through a pure resolver, and the project references `Timberborn.Gathering` plus `Timberborn.Cutting`.
- 2026-05-06 worker selected-Blueberry diagnosis: the previous live collector only accepted entities with a direct `Timberborn.Yielding.Yielder` component. The selected `Blueberry` exposes yield through `Gatherable.Yielder`, so registration never saw the harvestable and `burn_damage_registered_targets=0` was expected. Added Blueberry/Berries harvestable registration coverage, selected Blueberry resolution coverage, and empty-registration failure coverage.
- 2026-05-06 worker selected-Blueberry verification passed: `git diff --check`, focused `TimberbornBurnDamageStateTests|TimberbornQaCommandBridgeTests` with `46/46`, `dotnet build Wildfire.slnx --no-restore` with `0` warnings and `0` errors, full `dotnet test --no-build` with `173/173`, and `bun run typecheck`. Move to `04-verify` for focused review before rerunning live manual selected-Blueberry QA.
- 2026-05-06 review failed the selected-Blueberry fix: accepting `Cuttable.Yielder` without an explicit crop/harvestable gate can register tree/log targets into the crop-burn consequence path because `Log` is in the fuel catalog and crop-burn routing treats organic `Resource` targets as harvestables. Move back to `03-in-progress`; add a narrow collector filter plus regression proving cuttable tree/log targets stay out of `TWF-076` and remain in the `TWF-084` boundary.
- 2026-05-06 worker boundary fix on `~/repos/wildfire-TWF-076-qa`: added an explicit crop/harvestable gate around live yield resolution and excluded cuttable log resources from `TWF-076` selected-target, QA stimulus, and crop-consequence routing. `Gatherable` remains accepted for Blueberry, trees are rejected by `TreeComponent` or tree-name guard, and direct/cuttable yielders must be crop-named.
- 2026-05-06 worker boundary-fix verification passed: `git diff --check`, focused `TimberbornBurnDamageStateTests|TimberbornQaCommandBridgeTests|TimberbornCropBurnConsequenceTests` with `56/56`, `dotnet build Wildfire.slnx --no-restore` with `0` warnings and `0` errors, full `dotnet test --no-build` with `176/176`, and `bun run typecheck`. Blueberry/Berries registration and selected Blueberry resolution remain covered; cuttable Oak/Log registration, selected-log resolution, and crop-consequence routing now regress to no-op. Move to `04-verify` for focused review before rerunning live manual selected-Blueberry QA.
- 2026-05-06 review passed the boundary fix with no blocking findings. Reviewer confirmed live registration rejects `TreeComponent` or tree-name objects, keeps `Gatherable` Blueberry/Berries eligible, allows direct/cuttable yielders only when crop-named, and keeps organic `Log` resource targets out of selected-target resolution, automatic stimulus selection, and crop consequence routing. Verification passed: `git diff --check HEAD`, untracked collector whitespace check, focused tests with `56/56`, `dotnet build Wildfire.slnx --no-restore`, `bun run typecheck`, and full `dotnet test --no-build --no-restore` with `176/176`. Keep in `04-verify` for fresh live manual selected-Blueberry QA; do not move to integration until that gate passes.
- 2026-05-06 clean live manual Blueberry QA failed under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-076-manual-blueberry-boundary-fix-20260506T180948Z/`. QA clean-quit Timberborn, redeployed the reviewed branch from `~/repos/wildfire-TWF-076-qa`, restarted, and loaded `QA Tunnels and Booms - 2026-05-06 14h10m, Day 1-17.autosave` from `~/Documents/Timberborn/ExperimentalSaves/QA Tunnels and Booms/2026-05-06 14h10m, Day 1-17.autosave.timber`.
- 2026-05-06 clean live manual Blueberry failure detail: screenshots `screenshots/02-selected-blueberry-before-stimulus.png` and `screenshots/03-selected-blueberry-after-failed-stimulus.png` prove a hand-selected `Blueberry`, but `commands/05-qa-delta-stimulus-selected-blueberry-12cycles-requested.txt` failed with `Selected_Timberborn_object_did_not_resolve_to_a_registered_crop_or_harvestable_burn-damage_target.` Follow-up `status` and `qa-readiness` remained command-responsive but reported `burn_damage_registered_targets=0`, `burn_damage_registered_crop_burn_targets=0`, and all crop counters at `0`; no `selected_crop_target` or `12` cycle proof was accepted. Move back to `03-in-progress`; the unit-modeled `Gatherable.Yielder` fix still does not match the live selected Blueberry registration/resolution shape, so add live-shape diagnostics or resolve the actual Timberborn component path before rerunning QA.
- 2026-05-06 worker diagnostic unblock on `~/repos/wildfire-TWF-076-qa`: this is not a live crop fix. The worker found the command bridge was hiding useful runtime state on failing `qa-delta-stimulus` by returning placeholder state after an exception, even though follow-up `status` proved the runtime was loaded. Added bounded selected-object diagnostics for selection state, selected object type/name, block name, component types, occupied cell counts, yield-like debug tokens, and failure reason; `status` and `qa-readiness` now expose those diagnostics.
- 2026-05-06 worker diagnostic verification passed: focused `TimberbornBurnDamageStateTests|TimberbornQaCommandBridgeTests|TimberbornCropBurnConsequenceTests` with `58/58`, `dotnet build Wildfire.slnx --no-restore` with `0` warnings and `0` errors, full `dotnet test --no-build` with `178/178`, `git diff --check`, and `bun run typecheck`. Move to `04-verify` for focused review of the diagnostic unblock, then rerun manual Blueberry QA only to capture actionable live selected-object shape unless review finds a blocker.
- 2026-05-06 review failed the diagnostic unblock: `status`/`qa-readiness` still compute `burn_damage_registered_crop_burn_targets` with a private helper that treats every organic resource target as crop/harvestable, while selected-target and consequence routing use `TimberbornCropBurnTargetClassifier` to exclude `Log`. Move back to `03-in-progress`; make runtime crop-target counts use the shared classifier and add a regression proving registered `Log` targets do not increment crop-burn target or owned-cell counts.
- 2026-05-06 worker status-count consistency fix on `~/repos/wildfire-TWF-076-qa`: runtime `status` and `qa-readiness` crop burn registration counts now use `TimberbornCropBurnTargetClassifier` through a shared registered-target summary helper. Registered `Log` resource targets stay out of `burn_damage_registered_crop_burn_targets` and `burn_damage_registered_crop_burn_owned_cells`, while Blueberry support is preserved.
- 2026-05-06 worker status-count verification passed: focused `TimberbornBurnDamageStateTests|TimberbornQaCommandBridgeTests|TimberbornCropBurnConsequenceTests` with `59/59`, `git diff --check`, `dotnet build Wildfire.slnx --no-restore`, and `bun run typecheck`. Move back to `04-verify` for focused review of this narrow fix, then rerun manual selected-Blueberry live QA if review passes.
- 2026-05-06 review passed the status-count consistency fix with no blocking findings. Reviewer confirmed runtime status now derives crop target and owned-cell counts through `TimberbornCropBurnTargetClassifier.SummarizeRegisteredTargets(...)`, and the same classifier remains aligned across selected-target resolution, automatic QA stimulus selection, crop consequences, and diagnostics. Verification passed: `git diff --check`, untracked collector whitespace check, focused tests with `59/59`, and `dotnet build Wildfire.slnx --no-restore`. Keep in `04-verify` for live diagnostic/manual Blueberry QA, not integration.
- 2026-05-06 live diagnostic/manual Blueberry QA failed under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-076-diagnostic-manual-blueberry-20260506T184124Z/`. QA clean-deployed and restarted, loaded `QA Tunnels and Booms - 2026-05-06 14h17m, Day 1-18.autosave` from `~/Documents/Timberborn/ExperimentalSaves/QA Tunnels and Booms/2026-05-06 14h17m, Day 1-18.autosave.timber`, and manually selected a visible `Blueberry` with screenshot proof.
- 2026-05-06 live diagnostic result: the selected object is `Timberborn.SelectionSystem.SelectableObject` with object/block name `BlueberryBush(Clone)`, `106` components, one occupied in-grid cell, and yield debug tokens `Timberborn.Gathering.GatherableSpec:YielderSpec:GoodAmountSpec:Berries:3`, `Timberborn.Gathering.Gatherable:Yielder:GoodAmount:Berries:3`, `Timberborn.Gathering.Gatherable:Yielder:YielderSpec:GoodAmountSpec:Berries:3`, `Timberborn.Yielding.Yielder:GoodAmount:Berries:3`, and `Timberborn.Yielding.Yielder:YielderSpec:GoodAmountSpec:Berries:3`.
- 2026-05-06 live diagnostic failure detail: `commands/04-qa-delta-stimulus-selected-blueberry-12cycles.txt` failed with `Selected_Timberborn_object_did_not_resolve_to_a_registered_crop_or_harvestable_burn-damage_target.` The new diagnostics proved manual selection and yield discovery, but `burn_damage_registered_targets=0` and `burn_damage_registered_crop_burn_targets=0`, so no `selected_crop_target` or `12` cycle proof was accepted. Move back to `03-in-progress`; align live registration with the proven `BlueberryBush(Clone)`/`Berries:3` shape.
- 2026-05-06 worker selected-object fallback fix on `~/repos/wildfire-TWF-076-qa`: if a manual selection fails to resolve and the burn-damage registry is empty, the adapter now builds a crop/harvestable burn target from the selected block's footprint and yield components, registers its dynamic descriptor, then resolves it as `selected_crop_target`. This targets the proven live `BlueberryBush(Clone)`/`Berries:3` shape while preserving the tree/log boundary through tree-name/component checks and the shared crop target classifier.
- 2026-05-06 worker selected-object fallback verification passed: `git diff --check`, focused `TimberbornBurnDamageStateTests|TimberbornQaCommandBridgeTests|TimberbornCropBurnConsequenceTests` with `61/61`, `dotnet build Wildfire.slnx --no-restore`, full `dotnet test --no-build` with `181/181`, and `bun run typecheck`. Deterministic coverage now includes selected Blueberry registration/resolution and selected tree/log rejection. Move to `04-verify` for focused review, then rerun live manual selected-Blueberry QA if review passes.
- 2026-05-06 review passed the selected-object fallback fix with no blocking findings. Reviewer confirmed the fallback is adapter-side, only attempts dynamic selected-object registration after selected-target resolution fails and the burn-damage registry is empty, matches the live `BlueberryBush(Clone)`/`Berries:3` shape through yield components and footprint cells, and preserves tree/log gates through the shared classifier. Verification passed: `git diff --check`, focused tests with `61/61`, `dotnet build Wildfire.slnx --no-restore`, `bun run typecheck`, and `bun scripts/invoke-timberborn-command.ts --help`. Keep in `04-verify` for live manual selected-Blueberry QA.
- 2026-05-06 live manual selected-Blueberry QA passed under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-076-selected-object-fallback-qa-20260506T190341Z/`. QA clean-quit, deployed, restarted, and loaded `QA Tunnels and Booms - 2026-05-06 15h03m, Day 2-1.autosave`. Manual selection screenshots `screenshots/02-after-click-1017-741.png` and `screenshots/03-selected-blueberry-after-12cycles.png` show the in-game `Blueberry` panel.
- 2026-05-06 live pass proof: `commands/07-qa-delta-stimulus-selected-blueberry-12cycles.txt` reported `qa_delta_stimulus_target_source=selected_crop_target`, `selected_crop_target_state=resolved`, `BlueberryBush(Clone)`, `burn_damage_registered_targets=1`, and `burn_damage_registered_crop_burn_targets=1`. Follow-up `commands/08-qa-readiness-after-selected-blueberry-12cycles.txt` reported `qa_delta_stimulus_sustained_heat_completed_cycles=12`. `logs/Player-selected-blueberry-stimulus-window.txt` showed `crop_burn_considered_targets=1`, `crop_burn_burnable_targets=1`, and precise accepted safe-unavailable mutation telemetry `crop_burn_skipped_unsafe_apis=1`. Move to `05-integration` for integration review; accepted live result is safe-unavailable crop mutation telemetry, not live yield loss/death.
- 2026-05-06 integration review passed for `~/repos/wildfire-TWF-076-qa` branch `codex/TWF-076-crop-burn-consequences`, including dirty QA fixes and the untracked required file `src/Wildfire.Timberborn/TimberbornLiveCropBurnDamageTargets.cs`. Reviewer confirmed the accumulated implementation keeps crop consequence behavior adapter-local, preserves tree/log separation, bounds the selected-object fallback to the live selected crop/harvestable case, and caps diagnostic token output. Verification passed: `git diff --check`, untracked collector whitespace check, focused tests with `61/61`, `dotnet build Wildfire.slnx --no-restore`, full `dotnet test --no-build --no-restore` with `181/181`, `bun run typecheck`, and `bun scripts/invoke-timberborn-command.ts --help`.
- 2026-05-06 integration complete in the main checkout. The integration preserved overlapping Sprint 10 mainline changes while adding the crop-burn sink, selected-crop QA path, sustained-heat command support, and required `TimberbornLiveCropBurnDamageTargets.cs` collector. The accepted QA path remains manual in-game crop selection by hand, not script-selected crop discovery.
- 2026-05-06 integration verification passed in the main checkout: `git diff --check`, untracked new-file whitespace checks, focused `TWF-076` tests with `109/109`, `dotnet build Wildfire.slnx --no-restore`, `bun run typecheck`, full `dotnet test --no-build --no-restore` with `387/387`, and `bun scripts/invoke-timberborn-command.ts --help`.
