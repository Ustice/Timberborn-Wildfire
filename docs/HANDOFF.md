# Wildfire Handoff

## Current State

- Wildfire repository scaffolded in `~/repos/wildfire`.
- Core packed-cell, grid, GPU simulator contract, delta, listener, and change-registration types exist.
- CLI scenario preview exists with named seeded scenarios, seed/dimension/layer options, and packed-cell-derived rendering.
- Unity now has compute-buffer, shader-dispatch, change-upload, compact-delta, visual-field, and snapshot-harness scaffolds; it should remain the GPU adapter.
- Unity now has an opt-in batchmode shader execution harness that imports `FireSim.compute`, dispatches a seeded fixture, reads packed cells, compact deltas, and a visual checksum.
- Timberborn now has deterministic cell-mapping adapter scaffolds; it should remain a host adapter.
- Timberborn QA now has an in-process command bridge and a live Timberborn file binding with read-only `status`, `qa-readiness`, and `help` commands, searchable result tokens, unit coverage for the command safety boundary, and live `Player.log` proof from a loaded save.
- Timberborn menu coordinate capture now covers startup Mods, post-startup load, exit-to-main confirmation, standalone main menu, main-menu Load Game, in-game Escape, in-game Load Game, Mods dialog, and HUD targets at 1920 x 1080, with screenshot evidence in `docs/reference/screenshots/timberborn-menu-coordinate-guide/`.
- Timberborn debug/developer panel reference now exists in `docs/timberborn-debug-panels.md`, with clean 1920 x 1080 screenshots under `docs/reference/screenshots/timberborn-debug-panels/`.
- Stale `TWF-017` and `TWF-027` worktrees were inspected and removed. `TWF-017` contained older already-integrated startup-harness work, and `TWF-027` contained the contaminated screenshot already rejected in ticket notes.
- The deploy pipeline now emits Timberborn-facing `netstandard2.1` assemblies and has live `Player.log` proof that Timberborn discovers `Wildfire (v0.1.0.0)` and loads the `Wildfire testing` save without the earlier `.NET 10` `System.Linq` crash.
- Timberborn now loads the private `wildfire_compute_mac` bundle as a real `ComputeShader`, initializes the compute-backed simulator, and live `status` reports `simulator_integrated=true` with dimensions `128x128x23`.
- Live Timberborn evidence now includes real compute dispatch/readback tokens such as `wildfire_timberborn_gpu_dispatch_kernel`, `wildfire_timberborn_gpu_readback_completed`, and `wildfire_timberborn_dispatch_completed`.
- Timberborn gameplay-surface Sprint 2 is complete: material mapping has deterministic terrain/resource/building/water bands, the debug overlay updates from compact deltas, pausable buildings can be paused from fuel-depleted building deltas, and the QA water-suppression command proves a queued `SetWater=3` change through live GPU dispatch.
- The first coherent live Timberborn gameplay loop is QA-proven: guarded loaded-save attach, fixed fire stimulus, non-zero spread and visual updates, native quick-warning communication, compact-delta gameplay consequences, water-suppression resolution, delayed stability, copied `Player.log`, screenshots, and clear final QA lock state are preserved under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-046-live-20260502T232641Z/`.
- The old alternate C# execution path and its snapshot tests were removed; future simulation work should target the GPU simulator path only.
- Prometheus sprint lessons are incorporated as docs, role instructions, local skills, and a file-based ticket board, not as copied Prometheus runtime code.

## Current Ticket State

- The compute-first ticket set exists as `TWF-000` through `TWF-063`.
- `kanban/by-status` is the sprint source of truth.
- `TWF-000` through `TWF-008` are done and integrated on `main`.
- `TWF-009` is done and integrated on `main` in commit `aa9a260`; live status and `Player.log` evidence prove the Timberborn delta-consumer hook path is wired and reporting.
- `TWF-010` is done: runtime diagnostics now cover GPU lifecycle, queued changes, dispatch/readback timing, listener counts, and Timberborn adapter startup/shutdown without per-cell mapped-change spam.
- `TWF-011` is deferred until the new non-zero live delta sprint creates meaningful profiling evidence that full-grid dispatch is too expensive.
- `TWF-012` is done and integrated on `main` in commits `6043959`, `1feb25e`, and `499040d`.
- `TWF-013` is done with an explicit boundary: opening screens and standalone main-menu coordinates are split to `TWF-020`.
- `TWF-014` is done and integrated on `main` in commit `85e5538`.
- `TWF-015` is deferred because `TWF-030` supersedes the older load-latest-save utility shape with the fuller launch/load/unpause workflow and overlapping `scripts/**` scope.
- `TWF-016` is done: deploy works, copies `netstandard2.1` assemblies, and `Player.log` shows `Wildfire (v0.1.0.0)` after restart.
- `TWF-017` is done: after an approved Timberborn restart, the startup harness passed against current-window launch evidence and a loaded-save `status` command returned `simulator_integrated=true`.
- `TWF-018` is done and integrated on `main` in commit `5b80d04`.
- `TWF-019` is done and integrated on `main` in commit `a7ed538`; live Timberborn `status` invocation returned `wildfire_command_result success=true` from the `Wildfire testing` save.
- `TWF-020` is done: startup Mods, post-load, exit confirmation, standalone main menu, and main-menu Load Game coordinates are captured.
- `TWF-021` is done: the Timberborn compute-backed simulator factory now loads and initializes in live Timberborn.
- `TWF-022` is done: the AssetBundle compatibility blocker was resolved by the Unity project module/package fix proven in `TWF-023`.
- `TWF-023` is done and integrated on `main` in commit `9859ceb`; live evidence shows `simulator_integrated=true`.
- `TWF-024` is done: the contaminated in-game running screenshot was replaced with a clean `1920 x 1080` Timberborn loaded-save baseline.
- `TWF-025` is done: the bottom-menu guide covers the loaded-save toolbar, submenus, `Alt-Shift-Z` developer mode, `Shift-Alt-X` debug entry, and composite screenshot references.
- `TWF-026` is done: Bun-based TypeScript type checking now exists through `bun run typecheck`, with a minimal script-scoped `tsconfig.json` and passing mainline evidence.
- `TWF-027` is done: the contaminated first worktree capture was rejected, clean debug/developer screenshots were captured in the main checkout, and `docs/timberborn-debug-panels.md` documents the visible panels, QA relevance, and unsafe mutation controls.
- `TWF-028` is done: current Wildfire QA cannot navigate Timberborn menus remotely without screen interaction; the accepted safer path is narrow allowlisted command-bridge outcomes, not generic menu driving.
- `TWF-029` is done: the read-only `qa-readiness` command is allowlisted, documented, unit-tested, and live-verified after deploy/restart with `loaded_game_ready=true`, `simulator_integrated=true`, dimensions `128x128x23`, and an advanced tick.
- `TWF-030` is done: the signal-driven latest-save startup utility launches Timberborn, clears startup Mods and Experimental Mode from screenshot-confirmed signals, continues the latest save, recognizes the top-HUD loaded-save state, unpauses, and records `simulator_integrated=true` status plus dispatch evidence.
- `TWF-031` is done: the guarded `qa-delta-stimulus` command is implemented, unit-tested, live-verified, and recorded with non-zero GPU readback evidence.
- `TWF-032` is done by `TWF-031` evidence: live QA captured `wildfire_timberborn_gpu_readback_completed ... delta_count=2`, `wildfire_timberborn_dispatch_completed ... delta_count=2`, and follow-up `qa-readiness` with `last_delta_count=1`.
- `TWF-033` is done: the runtime binds the first Timberborn-facing consequence through the debug visual state delta-consumer sink and live evidence shows non-zero dispatch/consumer counters with persistent `last_delta_consumer_debug_visual_cells`.
- `TWF-034` is done: worker profiling evidence from the current `TWF-031`/`TWF-032` live artifact shows full-grid dispatch is acceptable on the observed `128x128x23` loaded save, so `TWF-011` should remain deferred for now.
- `TWF-035` is done and integrated on `main` in commit `9c22c83`: Timberborn material mapping now covers deterministic terrain, stockpile resource, vegetation, wood-like building, non-burnable building, wet-cell, and vertical footprint behavior.
- `TWF-036` is done and integrated on `main` in commit `7e7cee8`: compact fuel-depleted building deltas can pause matched unpaused `PausableBuilding` instances through a bounded Timberborn-owned consequence lane, with live QA proof from `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-036-retry-20260502T190222Z`.
- `TWF-037` is done and integrated on `main` in commit `5db2490`: the debug inspection overlay stores latest packed cell state from compact changed cells, exposes updated-cell counters, and has live QA proof with `debug_visual_updated_cells` above zero.
- `TWF-038` is done and integrated on `main` in commit `00e66bb`: the QA-only `qa-water-suppression-stimulus` command queues a bounded `SetWater=3` change, rejects arguments, exposes durable positive water-change evidence, and has live QA proof from `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-038-live-retry-20260502T194053Z`.
- `TWF-039` is done and integrated on `main` in commit `2a7f013`: the live compute simulator now binds the GPU `VisualFields` buffer to a Timberborn-resolvable visual-field surface singleton, exposes bounded inspection/consumer APIs, reports QA telemetry, and has live proof from `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-039-live-final-20260502T204610Z`.
- `TWF-040` is done and integrated on `main` in commit `808d88d`: compact-delta visual events now feed a bounded pooled Timberborn presentation layer using the `TWF-039` visual-field surface, expose active/updated/last-nonzero counters, resolve the native `CampfireFire` effect in live QA, and have proof from `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-040-live-final-20260502T211935Z`.
- `TWF-041` is done and integrated on `main` in commits `3d1b910` and `942251c`: fire/smoke/ash/visibility visual constants are tuned in the C# mirror and HLSL shader, accepted shader checksum scenarios are recorded for `single-ignition` and `line-of-fuel`, ash remains an explicit derived heat/fuel approximation without packed-cell storage changes, and live QA proof is under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-041-qa-20260502T215231Z`.
- `TWF-042` is done and integrated on `main` in commit `fde36e6`: compact-delta alert decisions now feed one bounded Timberborn quick-warning player alert, QA telemetry reports alert counters, and live QA proof under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-042-live-rerun-20260502T222801Z` shows `Wildfire alert: 1 new fire. Max heat 15.` with matching status counters.
- `TWF-043` through `TWF-046` are done for Sprint 4: game-feel tuning, release simulation decisions, exact release shader snapshots, and coherent live-loop QA are integrated with evidence.
- `TWF-046` live proof is under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-046-live-20260502T232641Z/`: the run proved loaded-save readiness, fixed fire stimulus, spread, visual/presentation state, alert/status communication, compact-delta gameplay consequences, water-suppression resolution, and delayed runtime stability with no new strict failure-scan hits.
- Sprint planning now lives in `kanban/sprint-planning.md`. Sprint 5 closeout lives in `kanban/sprints/sprint-05.md`, and the active Sprint 6 charter lives in `kanban/sprints/sprint-06.md`.
- The board-audit command is `bun run kanban:audit`; it reports dependency-ready tickets, blockers, write-scope overlaps, ticket hygiene gaps, broken symlinks, missing dependencies, and unexpected `kanban/` edits in sibling worktrees.
- Sprint 5 is closed as "Safety Rails And Test Surfaces." `TWF-047`, `TWF-049`, `TWF-065`, `TWF-093`, `TWF-094`, `TWF-095`, `TWF-096`, `TWF-114`, `TWF-118`, and `TWF-126` are done.
- `TWF-093` passed enabled-save reload QA on 2026-05-03. Evidence under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-093-enabled-save-reload-20260503T040447Z` proves pre-save fire activity, save slot `Wildfire testing (7)`, reload with Wildfire still enabled, recovered command/status output, post-reload stimulus with visuals/alerts, and a `0`-line critical scan.
- `TWF-094` passed disabled-mod recovery QA on 2026-05-03. Evidence under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-094-disabled-mod-recovery-20260503T041547Z` proves the player-facing Mods dialog disable path, Timberborn's missing-mod warning for `Wildfire testing (7)`, recoverable gameplay load with Wildfire disabled, expected command-bridge timeout while the mod is absent, and no disabled-load critical scan hits.
- `TWF-095` passed re-enable runtime rebuild evidence on 2026-05-03. Evidence under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-095-reenable-runtime-rebuild-20260503T050000Z` proves Wildfire re-enabled through the Mods dialog, `Wildfire testing (7)` loaded again, runtime and simulator rebuilt at `128x128x23`, command/status recovered after a transient command-race retry, and post-unpause `qa-readiness` advanced to `tick_count=4`.
- `TWF-047` accepted the save lifecycle parent gate from `TWF-093`, `TWF-094`, and `TWF-095`; no save lifecycle defect was exposed.
- `TWF-049` passed fresh review and live compatibility-probe QA on 2026-05-03. Evidence under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-049-compatibility-live-20260503T051809Z` reports `compatibility_probe_status=compatible`, `required_passed=5/5`, `optional_passed=8/8`, and `degraded_features=none` with real compute asset/runtime proof.
- `TWF-065` passed fresh review and live recording QA on 2026-05-03. Evidence under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-065-live-screen-recording-20260503T051842Z` includes one 10-second `1920x1080` high clip, one 12-second `1280x720` low clip, metadata JSON, command/log evidence, dry-run/no-activate proof, and final lock-state proof.
- `TWF-118` passed a fresh review after failed-review fixes and integrated the scenario-save generator. Evidence includes dry-run/generation/safety refusal checks under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/generated-scenarios/twf-118-re-review-generated-20260503T055438Z`; live load validation is intentionally split to `TWF-119`.
- `TWF-096` passed review and integrated the adapter-local release settings framework. The stable prefix is `JasonKleinberg.Wildfire.release.`, with `settings_schema_version` read from Timberborn `ISettings` as an integer-backed setting.
- `TWF-114` passed review and integrated the adapter-owned resource fuel catalog. Unknown Timberborn resource ids default conservatively, and downstream tickets should consume the catalog for stored goods, explosives, and infrastructure classification.
- `TWF-050` is blocked on a command-unresponsive normal-launched Timberborn session after deploy validation was fixed and reviewed. The older Steam launch-args prompt was explained by parameterized launch. Fresh Sprint 8 evidence under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/TWF-050-qa-20260503T125647Z` shows deploy passed, Timberborn launched normally without parameters, the compute bundle is present with checksum `290c097c12ec885263eb472e20a4741d9cfbb9a24f65a396baed57390982ee79`, but `qa-readiness --require-advanced-tick` timed out with no `command-outbox.txt`.
- `TWF-097` deterministic implementation passed fresh review after a failed-review loop caught and fixed a typed-settings bug. It remains blocked on live QA because disabled-state proof needs Timberborn launch to work.
- `TWF-064` and `TWF-119` are also blocked on the same live launch environment because both require a command-responsive loaded Timberborn save.
- `TWF-066` is done for the procedural Fire readability baseline. Live QA under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/TWF-066-qa-20260506T194339Z` proved plainly legible normal-gameplay Fire particles with `WildfireProceduralFireParticles`, active pooled Fire effects, debug GPU field renderer disabled, and zero presentation/material/visual failures. Smoke, ash, and steam tuning remain separate tickets.
- `TWF-088` has a reviewed deterministic implementation on `codex/TWF-088-fire-spread-pace` at commit `8eff5cf6adf85cf8729ab19c1abdb592a7f549e3`. It is blocked on live QA after deploy passed but `load-latest-save-and-unpause` timed out at unknown screens and `qa-readiness --require-advanced-tick` timed out waiting for `command-outbox.txt`; evidence root is `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-088-live-20260503T073516Z`, with manifest at `kanban/evidence-manifests/TWF-088-qa-20260503T073516Z.md`.
- `TWF-089` is done for the Sprint 10 fuel burn-down recovery. Low/medium/high burn-duration proof has been restored on the live board; downstream behavior tuning blockers remain `TWF-090`, `TWF-091`, and `TWF-092`.
- `TWF-090` has a reviewed deterministic evidence pass on `codex/TWF-090-water-suppression` at commit `79aa895778271819312f58e1159a10158aa289ad`. It is stacked on reviewed `TWF-089`, accepts the current water constants unchanged, proves dry direct-control versus wet direct-suppression shader behavior, and is blocked on upstream live acceptance plus its own live suppression recording, `qa-water-suppression-stimulus`, follow-up `qa-readiness` or `status`, copied `Player.log`, recording artifacts, and final QA lock/state evidence.
- `TWF-091` has a reviewed deterministic evidence pass on `codex/TWF-091-structure-vertical-fire` at commit `83af04b10a05ef192bd9461ca0b90ae35fff5abd`. It is stacked on reviewed `TWF-090`, accepts the current structure/vertical rules unchanged, proves bounded six-neighbor z-axis and cardinal building-cluster behavior, and is blocked on upstream live acceptance plus one live vertical or multi-cell structure recording with command/status/log artifact proof.
- `TWF-092` has a reviewed deterministic evidence pass on `codex/TWF-092-burnout-cooling` at commit `36e27d87e01ce786507db925debf954452204198`. It is stacked on reviewed `TWF-091`, accepts current burnout/cooling rules unchanged, proves single-ignition, line-of-fuel, and building-cluster cooling timelines, and is blocked on upstream live acceptance plus one live burnout/cooling recording with command/status/log artifact proof.
- `TWF-075` is done for Sprint 7 after two failed-review loops. The integrated burn-damage foundation keeps state Timberborn-local, exposes resource-accounting snapshot fields, adds burn-capacity telemetry, keeps `Wildfire.Core` host-agnostic, and passed main-checkout `git diff --check`, targeted burn-damage tests, full `dotnet test`, and `dotnet build Wildfire.slnx`.
- `TWF-116` is accepted as the explosive stored-good contract. Jason accepted `Explosives` and `Fireworks` as first-pass explosive stored goods, accepted `Biofuel`, `Grease`, and `PineResin` as volatile candidates when runtime ids are confirmed, accepted keeping dynamite/detonators out of stored-goods behavior as infrastructure cases, and required pausing or splitting inventory mutation if no safe inventory API is found.
- `TWF-072` is done for beaver field exposure telemetry. Live QA under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/TWF-072-qa-20260506T205748Z` proved the telemetry-only path with `qa-delta-stimulus beaver-exposure`: sampled beaver candidate targeting, sustained heat `12/12`, `exposed_beavers=2`, `respiratory_cells=8`, no position or bounded-sampling skips, and no beaver behavior/native mutation tokens.
- `TWF-076` is done for crop/harvestable burn consequences. Live QA under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-076-selected-object-fallback-qa-20260506T190341Z` used manual in-game Blueberry selection by hand, not script crop discovery, resolved `selected_crop_target`, completed sustained heat `12/12`, and accepted precise safe-unavailable crop mutation telemetry.
- `TWF-084` is done for tree/cuttable burn consequences. Live QA under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/TWF-084-tree-burn-live-20260506T203532Z` proved `1250` registered tree targets, a real `center-tree` target, nonzero tree-burn considered/burnable counters, and precise safe-unavailable native tree mutation telemetry.
- `TWF-152`, `TWF-153`, and `TWF-154` are done for the first explosive infrastructure slice. Dynamite triggering remains separate from detonator safety and tunnel fire behavior. `TWF-153` accepted the conservative same-cell `detonator-dynamite-control:*` fallback with `Disarm()`-only behavior, `disabled=2`, `recoverability_preserved=2`, `armed=0`, and no forbidden `Trigger()`, `TriggerDelayed()`, `Arm()`, or `Evaluate()` evidence.
- `TWF-117` is accepted as the infrastructure classification contract. Jason accepted zero-cost paths as non-burnable safe no-ops, burnable construction-resource infrastructure as burnable or partially burnable, water-passing infrastructure as difficult to burn, and tunnels/dynamite/detonators as separate explosive or destruction cases.
- Follow-up tickets `TWF-127` through `TWF-130` now cover path infrastructure fire effects, power infrastructure fire effects, water infrastructure fire effects, and tunnel/dynamite destruction design.
- `TWF-139` and `TWF-146` are done. Live 50x50 Diorama import proved real entity-backed counts, and generated scenario manifests now include real field checkpoints with packed-cell and companion expectations.
- `TWF-141` passed the importer source-parity gate after `TWF-151`. Evidence under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-141-parity-retry-20260505/` includes the 50x50 Diorama JSON fixture, binary cell export, metadata, live `qa-readiness` transcript, and parity report. Dimensions and all source counts match; resolved-cell counts remain diagnostic-only because the snapshot exporter does not replay Timberborn's exact runtime placement and overwrite order.
- `TWF-147` is done. Live QA on 2026-05-05 proved the GPU field renderer over the loaded 50x50 Diorama save. Evidence under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-147-live-20260505T030230Z/` includes attach/unpause proof, command transcripts, `recording-high-fuel/2026-05-05T03-03-57-990Z-high/recording.mov`, extracted frame `twf-147-recording-frame-10.5s.png`, copied `Player.log`, and `high-fuel-renderer-tokens.txt`. The key live token is `wildfire_timberborn_gpu_field_renderer_updated tick=260 visible_regions=1 updated_regions=1 ... material_failures=0`, with follow-up `qa-readiness` proving `visual_field_surface_cells=57500`, `gpu_field_renderer_material_ready=true`, and `gpu_field_renderer_last_nonzero_updated_regions_tick=260`.
- `TWF-155` is done. Renderer telemetry now reports below-threshold culls separately as `invisible_regions` / `gpu_field_renderer_invisible_regions`, keeps `dropped_regions` for binding or capacity loss, and aligns status `gpu_field_renderer_updated_regions` with the log's rendered `updated_regions` value.
- `TWF-078` is accepted for the clean simulator-backed ash read-model and persistence route after Jason's 2026-05-20 manual confirmation. The split tainted-ash live gate is accepted in `TWF-166`.
- `TWF-082` is accepted for the clean fertile-ash collection/application route after Jason's 2026-05-20 manual confirmation. Tainted ash collection blocking and contaminated-cell behavior are accepted in `TWF-166`.
- `docs/ash-simulation-model.md` supersedes the older split between visual ash and gameplay ash. `TWF-157`, `TWF-158`, `TWF-159`, and `TWF-160` are done after Jason's 2026-05-20 testing confirmation: ash amount/contamination live in simulator transport state, Timberborn actions queue simulator ash mutations, renderers read simulator ash presentation state, and persistence/status/harvest/application sync with simulator ash. Remaining ash-model ticket is `TWF-161` for post-migration naming cleanup.
- `TWF-166` is done for the narrow tainted-ash live proof. Live QA from `~/repos/wildfire-TWF-166-tainted-ash-live` used `qa-delta-stimulus contaminated-tree` on `Fuel`, produced `ash_field_tainted_cells=1`, skipped clean growth with `ash_field_growth_skipped_tainted_cells=1`, skipped fertile collection with `fertile_ash_collection_skipped_tainted_or_spent_cells=1`, and preserved safe-unavailable native contamination telemetry with `tainted_ash_poison_skipped_no_safe_api=1`. Evidence root: `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-166-live-20260520T033350Z/`.
- `TWF-079` has reviewed implementation work parked on `~/repos/wildfire-TWF-079-contamination-aware` / `codex/TWF-079-contamination-aware`, but the required live QA gate is blocked. The first review rejected over-broad suppression counters; the repair passed review by separating map-presence water-like counters from actual suppression inputs and keeping contaminated burn-source counters distinct from affected-cell counters. QA deployed successfully, but Timberborn launched with `windows=0` and never reached a command-responsive loaded save, so no live behavior claims are accepted. Evidence root: `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-079-contamination-aware-qa-20260520T040919Z/`. Steam is clean suppression vapor only. `TWF-166` closed the narrow live tainted-ash proof, and `TWF-167` owns ash/water interaction.
- `TWF-169` has reviewed implementation work parked on `~/repos/wildfire-TWF-169-burned-tree-stumps` / `codex/TWF-169-burned-tree-stumps`, but the required live QA gate is blocked on the same live-window/command bridge availability issue. Review accepted the narrow native `NaturalResourceModel.ShowCurrentModel()` refresh path and no-success-on-failure counters. QA deployed successfully, but `load-latest-save-and-unpause` timed out behind `frontmost_bundle_id=com.openai.codex`, `status --wait=5` timed out waiting for `command-outbox.txt`, and Computer Use reported `cgWindowNotFound` for Timberborn. No tree burn/stump behavior was exercised. Evidence root: `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-169-burned-tree-stumps-qa-20260520T043017Z/`.
- Sprint 10 recovery is closed as mixed done/blocked. The active recovery gates closed in `06-done`: `TWF-066`, `TWF-072`, `TWF-076`, `TWF-084`, `TWF-089`, `TWF-152`, `TWF-153`, and `TWF-154`. Remaining blocked tickets are explicit in `07-blocked`.
- `TWF-051` and `TWF-071` are dependency-ready according to the audit but intentionally remain in `01-todo/`; they belong to later sprint slices unless Jason explicitly pulls them forward.
- `TWF-052` through `TWF-063` remain release-packaging work and should wait until gameplay consequences, evidence, and release media are stable.
- `TWF-066`, `TWF-067`, and `TWF-068` are accepted fire/smoke/ash visual baselines. `TWF-070` remains the steam visual tuning ticket after `TWF-162`.
- `TWF-075` through `TWF-081`, `TWF-084` through `TWF-087`, and `TWF-114` through `TWF-119` now cover world consequences, beaver danger, resource accounting, generated scenario saves, stored goods, explosives, infrastructure, ash, contamination, persistence, and feedback. `TWF-079` through `TWF-081` should account for the completed `TWF-157`/`TWF-158`/`TWF-159`/`TWF-160` ash-authority/presentation work, completed `TWF-166` tainted-ash live proof, and remaining `TWF-161` naming work. `TWF-168`, `TWF-169`, and `TWF-170` add the structure-on-fire alert, burned-tree stump bug, and paused-load ash visibility bug to the release-readiness backlog.
- `TWF-083` and `TWF-120` through `TWF-125` remain deferred future gameplay: overgrowth, faction fire response, fans, and fire berms. `TWF-082` moved out of deferred because fertile ash collection and application code now exists.
- QA now owns live Timberborn builds, deployments, launches, restarts, and the shared deploy/QA lock for verification runs.
- Required QA is a hard integration gate. If a ticket fails required QA, it cannot move to `05-integration/` until the same gate later passes with evidence.
- Required review is also a hard integration gate. If a ticket fails review, it must return to `03-in-progress/` for fixes, then move back through `04-verify/` and pass a fresh review before `05-integration/`.

## Next Exact Action

- Sprint 12 has started. `TWF-166` is accepted and integrated in the main checkout after review plus live QA; the artificial `qa-delta-stimulus tainted-ash` fallback exists only as a downstream tainted-ash behavior route, while the accepted gate used the natural `contaminated-tree` route.
- `TWF-161` is accepted and integrated in the main checkout after review plus deterministic verification. The rename keeps shader, asset-bundle, persistence, and snapshot compatibility names stable while moving internal C# concepts toward transport/material fields and ash presentation wording.
- `TWF-162` is accepted and integrated in the main checkout after review plus deterministic and opt-in Unity shader harness verification. Steam remains clean simulator-owned transport state; incoming moved steam now accumulates and caps through the packed transport field, with a convergence regression proving two sources do not erase one another.
- `TWF-170` is accepted and integrated in the main checkout after review, targeted post-merge tests, and live paused-load QA. Evidence under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-170-paused-ash-rerun-20260520T060148Z/` used an existing-ash `Fuel` autosave; before unpause, whole-scene inspection showed ash visible and status reported `ash_field_entries=750`, `visual_field_surface_bound=true`, and `gpu_field_renderer_material_ready=true`; after unpause, ash stayed stable with `ash_field_entries=750`, `fertile_ash_collected_goods=0`, and `fertile_ash_collection_depleted_cells=0`. Bulky screenshots were deleted after inspection per Jason's request, leaving the manifest, command status files, logs, copied `Player.log`, and cleanup proof.
- `TWF-079` is accepted and integrated after review, targeted post-merge tests, and live command-responsive QA. Evidence under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-079-contamination-aware-rerun-20260520T061602Z/` used `Fuel - Fuel` after Jason loaded a save with trees to burn; the proof preserved no native decontamination (`native_decontamination_attempts=0`), badwater/contaminated-water map telemetry with actual suppression inputs truthfully zero or safe-unavailable, contaminated-tree heat behavior, and tainted ash aftermath (`ash_field_tainted_cells=1`; `Player.log` tick 230 recorded `ash_field_contaminated_affected_cells=1`). Bulky startup screenshots were deleted after proof compaction, leaving manifests, command transcripts, deploy/load logs, copied `Player.log`, and cleanup proof.
- `TWF-085` has a reviewed deterministic implementation preserved on `codex/TWF-085-beaver-smoke` commit `4bdff943`, but it is blocked on live QA because Timberborn is not currently command-responsive. The implementation accepts only normal-smoke accumulation, reversible coughing safe-no-op, recovery decay, bounded batch/cooldown counters, and skipped-unsafe choking/death candidate telemetry; no native beaver health, work, pathing, incapacitation, or death API is invoked.
- `TWF-070` has a reviewed deterministic implementation preserved on `codex/TWF-070-steam-visual` commit `07875a16`, but the 2026-05-20 live QA rerun is still blocked on visual readability. Evidence under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-070-steam-live-20260520T060819Z/` proves the branch deployed, the steam tuning token logged, and water suppression processed after unpause with `last_positive_water_changed_count=1`, `last_delta_consumer_visual_effect_events=1`, `last_delta_consumer_visual_effect_failures=0`, and `gpu_field_renderer_material_ready=true`. The rendered scene was not acceptable: the loaded `Fuel` autosave had `ash_field_entries=750`, and extracted high-resolution frames were dominated by persisted ash plus active fire and gray/black smoke rather than distinct clean white steam.
- Next exact Sprint 12 action is to continue rerunning live QA now that Timberborn is command-responsive, starting with one blocked branch at a time. `TWF-070` needs either a cleaner loaded baseline or more visual tuning before it can be accepted; the next practical candidates are `TWF-085`, `TWF-163`, or `TWF-169`.
- The current Sprint 12 ready lane is `TWF-087`; `TWF-070`, `TWF-085`, `TWF-163`, and `TWF-169` still need live QA reruns, `TWF-167` is unblocked by `TWF-079`, and `TWF-168` waits on `TWF-080`/`TWF-163`. Overlapping `src/Wildfire.Timberborn/**`, `src/Wildfire.Unity/**`, and `tests/Wildfire.Core.Tests/**` scopes should be serialized.
- If continuing the real-field pipeline, `TWF-141`, `TWF-147`, and `TWF-155` no longer block source-count importer parity, GPU field renderer proof, or renderer telemetry clarity. Do not block Sprint 9 on generated-scenario technicalities if a better stable save proves the same real-field behavior. `~/Documents/Timberborn/ExperimentalSaves/Fuel/Fuel.timber` and `~/Documents/Timberborn/Maps/Fuel.timber` exist locally and were inspected on 2026-05-06 as `50x50`, so they remain useful smoke-test saves. Keep resolved-cell replay diagnostic-only unless a future ticket explicitly needs cell-identity-perfect snapshot parity.
- `TWF-133` is in `04-verify`: `Fuel` is now the Sprint 9 stable real-field acceptance surface candidate. Static inspection proved the `50x50` save shape, water/badwater, dense fuel, structures, paths, and storage contents; the 2026-05-06 live QA run loaded the save and proved command-responsive real-field simulation evidence under `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-144-148-live-20260506T071808Z/`.
- `TWF-144` and `TWF-148` are blocked after the 2026-05-06 live QA run. Positive behavior telemetry exists: `slow-reactable` active, 25 imported center-tree changes queued, `last_delta_count=50`, `started_burning=24/25`, water-change proof, and cooling back to `last_delta_count=0`. The gates failed because normal gameplay recordings did not visibly show readable fire/smoke/ash/steam/heat haze; `TWF-148` also reported `gpu_field_renderer_enabled=false` and `gpu_field_renderer_material_ready=false`.
- Next exact unblock is visual, not generated-scenario work: make the normal gameplay visual path plainly visible or provide a supported camera/renderer enablement path, then rerun `TWF-144` and `TWF-148` on `Fuel` or another stable real save.
- `TWF-149` remains blocked as the real-field consequence bridge until `TWF-144`/`TWF-148` pass. `docs/world-consequence-first-pass.md` remains the planning surface for scorch heat history, burned texture asset pipeline, simulator-backed ash presentation and adapters, tightened crop/tree/structure burned states, fertile ash growth, contaminated ash, persistence, and later beaver/player-feedback follow-ups.
- `TWF-077`, `TWF-115`, `TWF-127`, `TWF-128`, and `TWF-129` are done and integrated on `main` in commits `4323e57` and `573db63` after implementation, live QA, and fresh local review for the `TWF-075` ownership bypass fixes. `Fuel` live QA proved owned structure, storage, and path targets. `Smolville` live QA proved owned power and water targets after the infrastructure classifier started registering power/water names as infrastructure and the QA selector stopped cross-selecting the wrong infrastructure prefix.
- `TWF-156` is the Sprint 10 setup task for a reusable `256x256` map/save. It should prove a normal local forest fire inside a `256x256` world does not make the map chug; it does not require a whole-map burn and it does not need to be the first Sprint 10 ticket.
- Keep live-QA tickets blocked until Timberborn is closed/restarted normally and `bun scripts/invoke-timberborn-command.ts qa-readiness --wait=6 --require-advanced-tick` returns from a loaded save.
- Current blocked behavior/real-field tickets are `TWF-090`, `TWF-091`, `TWF-092`, `TWF-144`, `TWF-148`, `TWF-149`, and `TWF-150`. Do not imply progress on these until their recorded blockers are addressed.
- If continuing behavior tuning before live QA is fixed, stack dependent behavior work on the latest reviewed deterministic branch and keep it out of integration until upstream live gates pass.
- Do not treat visual tuning as one ticket: `TWF-066`, `TWF-067`, `TWF-068`, `TWF-070`, and `TWF-069` intentionally split fire effect, smoke effect, ash effect, steam effect, and fire behavior tuning so each can use the right recording evidence after `TWF-065`. Fire/smoke/ash are accepted; steam and broader fire-behavior recording gates remain.
- Beaver reactions can now start with `TWF-073` because `TWF-071` and `TWF-072` are accepted. Keep the telemetry-only boundary from `TWF-072`: behavior, injury, pathing, or native mutation belongs to the staged behavior tickets and must have fresh QA evidence.
- Keep `TWF-015` deferred unless `TWF-030` is abandoned; it is superseded by the newer startup/load/unpause utility.
- Keep `TWF-011` deferred for the first release. `TWF-051` reviewed the dispatch evidence and chose full-grid dispatch as the release baseline. `TWF-034` worker profiling over `~/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-031-live-20260502T143543Z/Player.log` observed ticks `1..43`, `43` full-grid dispatches, median dispatch wrapper time `2.737 ms`, p95 `4.846 ms`, max `6.014 ms`, and `SimulateFullGrid` kernel timing median `0.019 ms`, p95 `0.039 ms`, max `0.057 ms`. `TWF-046` later proved the coherent live loop on a loaded save without dispatch/readback cost becoming the visible blocker. Revisit active frontier only after post-release profiling on a larger map, sustained high-delta workload, or consequence-heavy live run shows a measured bottleneck.
- Use `bun scripts/summarize-dispatch-profile.ts "$HOME/Library/Application Support/Mechanistry/Timberborn/WildfireQA/twf-031-live-20260502T143543Z"` to regenerate the TWF-034 timing summary from the preserved artifact.
- Use `bun scripts/invoke-timberborn-command.ts qa-readiness --wait=6 --require-advanced-tick` against a loaded, unpaused Timberborn save when you need to confirm the in-game Wildfire command bridge, loaded-game runtime, simulator integration, and advancing dispatch tick are alive.
- Use `bun scripts/invoke-timberborn-command.ts qa-water-suppression-stimulus --wait=6 --require-advanced-tick` followed by `bun scripts/invoke-timberborn-command.ts qa-readiness --wait=6 --require-advanced-tick --require-water-changed` when you need to re-prove the suppression-registration path.
- Use `bun run typecheck` before accepting TypeScript script changes.
- If shader behavior changes, use the opt-in Unity harness command documented in `docs/TEST_PLAN.md` to prove real execution before accepting the change.

## Known Gaps

- `FireSim.compute` has local Unity batchmode proof, but CI does not run it unless Unity Editor licensing and compute-shader capable graphics are available.
- Historical TWF-009 evidence had `delta_count=0`; TWF-031 now proves the live stimulus, GPU readback, dispatch, and status path can report non-zero deltas.
- The player-legibility sprint is complete: GPU visual-field binding, bounded pooled native presentation, visual tuning evidence, and one player-facing alert/status surface are integrated and live-verified.
- During `TWF-046`, two `qa-building-burnout-stimulus` attempts produced burned-out alert/status evidence, and the first matched one building cell, but `building_burnout_applied_consequences` stayed `0`; `TWF-064` tracks this pausable-building pause mismatch without reopening the coherent-loop pass.
- TWF-030's live run proves the guarded startup/load/unpause workflow, but the broader Timberborn UI is not exhaustively mapped; add settings/new-game/map-editor/deeper Mods coordinates only through explicit QA passes.
- `TWF-009` intentionally provides Timberborn adapter hook surfaces; concrete binding to Timberborn damage/effect/alert services is follow-up work.
