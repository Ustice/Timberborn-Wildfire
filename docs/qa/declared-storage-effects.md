# Exact declared storage effects

Source checkpoint `0a0d268`. Full native suite: **1,282 passed**, zero failed/skipped. This is adapter/sink integration, not material admission or live colony proof.

The owned aggregate derives each constructed owner's inventory registration from its canonical Guid/family and complete original OWNED4 declarations. Missing original evidence yields explicit unavailable storage; an explicit empty declaration set means no inventory and makes no guessed native lookup. The storage-only wrapper accepts explicit registrations and rejects re-registration that changes original declarations. Legacy WF1 cell-routed storage remains untouched.

The native API resolves exact current Guid/liveness and whole declared topology before every read and withdrawal. Captured original role/name sets must match, and each actual Inventory must belong to its exact body. Supported constructed-owner roles are Stockpile, SimpleOutput and Manufactory. Other roles report unavailable. Enabled inventory reads use native UnreservedStock, reaching private factory inputs that output-oriented UnreservedTakeableStock omits; disabled inventories remain unavailable for mutation even though physical capture includes their goods. Each write rechecks native UnreservedAmountInStock and returns only after actual TakeConsumed completes. A thrown callback has no receipt/refund/replay. Native counters and net before/after quantity are not used as commit evidence.

Available named rows are summed by GoodId with checked arithmetic before the existing owner/resource fuel-credit selector runs once. Selected amounts split deterministically by native role then exact name; they do not acquire another budget per inventory. Every withdrawal keeps its planned exact declaration and re-resolves current eligibility. Short completed receipts contribute only actual removals to hazards; unused requested quantity is not automatically refunded or reselected. Existing fractional credit survives history under the same owner/resource key. The native hazard sink sums split rows and emits one blast/pulse per contributing fire decision, so same-good named withdrawals do not duplicate blasts.

Public aggregate and storage-only preflight execute inside the **same** resource guard's CaptureAtRest. Adapter callbacks cannot save, enter resource mutation, or reenter registration/delivery. A preflight read exception leaves body state unchanged and the guard save-safe. Simulator-owned ConsumeStep retains its existing outer transaction and invokes the same preparation without a nested guard. Body effects and every native mutation/hazard callback remain inside that outer mutation guard. Invalid status enums and inconsistent removal quantities throw instead of reporting successful/unavailable receipts.

Validation includes:

- Same good across two named inventories with budgets below, equal to and above one unit; deterministic split, one fractional owner credit, capture/restore of that credit, and disabled inventory exclusion.
- Actual installed native input Inventory/Manufactory methods: unreserved private stock is readable and TakeConsumed preserves an existing reservation. Existing actual native callback-throw tests remain green; native statistics are not receipts.
- Whole topology mismatch before withdrawal; callback depletion of a later row; hazards only for completed removals; missing witness versus explicit empty inventory set; unknown receipt status fail-stop.
- Public preflight save/mutation/reentry rejection for aggregate and standalone routes, read-only failure followed by safe retry, and existing step-delivery/save/failure tests.
- Complete session tests now supply explicit original declaration observations and native-domain fixture evidence. They retain actual receipt/credit progress through paired codec/new-session restore. Separate explicitly named legacy-witness fixtures keep diagnostic OWNED3 static-definition tests; no missing evidence is inferred or upgraded to make tests pass.

The bodies/inventories in routing tests are synthetic adapter fixtures; native managed tests execute selected actual methods over supplied backing state. Full native component ownership, concurrent callbacks during actual live role transitions, two-inventory end-to-end effects, recovered final-unit deletion, and a running colony remain controller gates. The prepared actual Unity two-inventory capture probe is separate and unexecuted at this checkpoint. Multiple-inventory/new-role material compilation remains gated pending the corresponding end-to-end integration. No fuel capacity policy, extra persisted role ledger, recovered/equipment policy or production activation is introduced.
