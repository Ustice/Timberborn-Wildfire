# Complete OWNED4 restored-world staging

Source: `557b984`, with final callback fidelity correction `a3c36fb`, on the OWNED4 witness/codec dependencies `403998d` and `f6be17f`. This is unpublished session preparation, not production loading or ordinary-step readiness.

## Authority and observation

Outer WF2 is unchanged. Private OWNED4 requires original declared inventory identities for every canonical owner, including an explicitly empty set and retained static witnesses for retired tombstones. OWNED1/2/3 remain diagnostically readable with unavailable complete compatibility; current native facts cannot upgrade missing original evidence. Original verified initial capture forms OWNED4. The old WorldSession restore is explicitly named `PrepareDiagnosticRestore` and stages neither desired projections nor a rich baseline. Its body-only observation cannot verify OWNED4 inventory identities; codec inspection remains available without inventing those observations.

`PrepareCompleteRestore` holds the existing resource guard's `CaptureAtRest` scope across current full-world and exact saved-Guid observations, static witness matching, body/history construction and new backend allocation. Saved accounting and BURN remain body authority. Current remaining yield or stock does not resize capacity or replay past loss. Every supplied original inventory declaration must match current exact role and component name. Static evidence is separate from physical stock and effect eligibility. Disabled inventories retain physical `Stock`, including reservations; `e454dd5` removes a restore-only rejection that contradicted initial material capture. Disabled yield remains distinct because its public amount can hide surviving raw native quantity.

One material-only compiler produces current supported native definitions without initial accounting selections or new material units. A new registry receives the current projected material baseline, original durable bindings and every supported retained contributor, including hidden contributors. Reconciliation may not allocate incidental bindings. Complete restore uses only `MaterialBaseline`: initial wetness and soil overlays never overwrite saved GPU fields. Active material, archives, known slots and ordered pending commands remain the saved simulator's exact authority.

Current membership must agree with saved retained owners; new owners, unproved excluded/terminal states, unproved disabled yield and unsupported inventory/material roles reject explicitly. The later [known-tree material slice](known-tree-material-restore.md) admits only verified ordinary retained wood while withholding Fresh eligibility; it preserves exact simulator authority. No exclusion becomes suspension or retirement. Actual registry absence and canonical retirement remain separate lifecycle mechanisms. Tree, Crop, spec-specific Vegetation, Structure and Stockpile material definitions are supported where their exact profiles and role evidence are supported; unknown profiles, Infrastructure and standalone GoodStack remain explicit gaps. Manufactory, RecoveredGoodStack and WardenStation declarations are observable evidence but are not thereby admitted as new material/effect roles.

## Finalization and proof

All supplied callback-capable native/history/factory work completes before one final exact snapshot comparison. A supplied callback cannot mutate cells, archives or pending input after the factory's initial fidelity check and still publish a session. Backend `CaptureSnapshot` itself is the trusted read contract; there is no unbounded alternation of native/backend observations. Failure disposes the new unpublished backend, releases the read scope and does not poison an otherwise safe world. Actual mutation failures still use the existing unsafe-save guard.

The complete fixture preserves two overlapping native owners, a hidden archive, original capacity 15 with current quantity 3, pending water input, saved wetness and Soil/Water/Badwater/Solid baseline definitions. It checks byte-equivalent paired recapture, no native yield/stock calls, exact binding reuse, legacy evidence rejection, unsupported membership/state/role rejection, read-scope reentry and callback drift disposal. Six final callback cases span initial, diagnostic and complete staging. Two complete-restore counterfactuals failed against the earlier check with no exception, then passed with final fidelity validation.

At `a3c36fb`: **45 focused cases and 1,200 full native tests passed**, zero failures/skips. Evidence: `/tmp/wildfire-complete-restore-authority-red.log`, `/tmp/wildfire-complete-restore-refinement.log`, `/tmp/wildfire-complete-restore-final.log`. Counts describe this isolated dependency graph; parent integration has additional tests.

The disabled-positive-GoodStack regression passes with unchanged stock and paired encoding, preserved desired material and zero inventory calls. After `e454dd5`, **19 complete-restore cases and 1,201 full native tests passed**, zero failures/skips; evidence is `/tmp/wildfire-disabled-stock-restore.log` and `/tmp/wildfire-complete-restore-stock-full.log`.

These tests execute the real registry, body accounting, history/codec, projection and staging code with supplied facts and an in-memory snapshot backend. They are not actual OWNED4 engine formation/load proof. Earlier [native definition](owned-native-restore.md) and [initial session](owned-initial-session-proof.md) reports remain historical OWNED3 evidence. Separate actual backend, retained-native-object and desired-reconciliation shader proofs retain their own narrower scopes.

`CompleteStaged` means desired-world evidence is present. A runtime still must execute validated material reconciliation with native consequence delivery under the same guard, handle retired active GPU owners, and publish without intervening native mutation. No ordinary runtime step, activation, quantity write, gameplay material-budget choice or release policy was added here.

## Native lifecycle observation evidence

`4b91e7a` requires the actual `LivingNaturalResource` component when the retained native shape is Tree, Crop or Vegetation; structures may legitimately lack it. The installed `NaturalResourcesConfigurator` adds that decorator to `NaturalResourceSpec`. All seven tree, twelve crop and three bush root BlockObject templates in the inspected archive carry that spec. Six focused tests execute actual native cached component lookup, distinguish missing components from both live/dead native state, and scan the installed templates. The full native suite passed **1,207 tests**, zero failures/skips; `/tmp/wildfire-restore-lifecycle-observation.log` and `/tmp/wildfire-restore-lifecycle-full.log`. This is native managed component-cache evidence, not a complete GameObject lifecycle; the controller's new OWNED4 probe owns the positive and missing-component engine cases.
