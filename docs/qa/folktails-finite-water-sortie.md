# Connected finite Folktails water sortie

Source `c8dfd88`, finite-command transport `2599d5f`; save/pause transport `6b8164f`. This connects the explicit borrowed-workplace offer, native fixed water source, private equipment inventory, shared water dispatch and return recovery. It is a QA-admitted playable prototype awaiting its physical game proof. The Fire Bell remains an art preview: no shipping bell placement, coverage, donor selection, alarms or recurrence is implied.

Dependencies: shared water producer/settled collector `9d2d5fb` / `eb04563`; native source factory `e4cbab9` / `c739dd4`; native conversion `131ba4c` / `3847807`. Their native and CPU engine limits remain recorded in [factory/restore proof](native-water-source-factory.md) and [conversion proof](warden-natural-source-fill.md).

## Connected ownership

QA supplies an exact donor workplace, stationary source, dry shore point, burning cell/safe approach and return Inventory owner/ComponentName. ArmWater prepares the recovery root for the donor's **current initialized employees**, outside native arbitration. The native manager enumerates its root list: installing a root from Decide could invalidate the enumerator when an offer subsequently declines. PrepareRecovery leaves current employment/executor and unassigned recovery inert; BindReturn now only binds an already prepared root. Newly assigned employees decline until a new explicit offer prepares them.

At the native between-job boundary, the selected worker keeps its employer and transfers to the existing beaver-owned, non-IJobBehavior executor. Recovery is bound before movement or pickup. The explicit prototype retains the earlier sixteen-unit envelope and two-game-hour deadline; fire approach uses the existing Warden two-unit horizontal working-distance envelope. These are finite fixture constraints, not faction balance or automatic target selection.

Phases append to the old four values: FetchingWater → AwaitingCredit → ApproachingFire → AwaitingApplication → Returning. Actual Transform.position, native stopping proximity, current source/shore geometry and fire safety establish arrival. Waiting at either destination rereads actual position safety. A stopped Walker's Success alone cannot authorize pickup/application.

**Character Tick never withdraws water.** Actual entity ticks occur while native water work may be parallel. The existing water-credit boundary first finishes its own guarded credit, then coordinator CollectNaturalWater visits collectors derived from the existing responder list. Each source conversion owns its separate existing transfer guard; no nested guard, new queue or scheduler exists. Loaded phase publication occurs inside that conversion; actual movement starts on the next owned character tick.

Native WaterInput retains fractional volume. Conversion uses the installed one-good quantum (0.2), demands at most one full quantum per settled attempt, and produces native Water1 only from sufficient credited clean volume. Dirty/mixed source admission is excluded from this prototype; the shipping mixed-water choice remains pending. No epsilon, authored credit, parallel-buffer read or duplicate cargo balance is introduced.

The same coordinator Water kind handles Warden and Borrowed offers. AwaitingApplication prepares AddWater3. A committed callback rechecks exact trip, manager, phase, actor, physical arrival and native Water1, calls native TakeConsumed, verifies the empty inventory/current actor, then publishes return. It schedules no navigation inside the GPU callback. A prepared null/full queue consumes nothing and permits exactly one ordinary step with no later producer fallback. Exceptions after native mutation retain the existing fail-stop/save exclusion semantics.

Cancellation after pickup abandons application and leaves the native bucket plus independent return binding for existing return-only arbitration. That walk holds no capacity reservation; actual arrival owns a temporary exact capacity and public reserved transfer. Full/missing storage preserves cargo and retryable binding. Empty or consumed trips attempt the existing safe origin return. This does not prove that every subsequent civilian route is fire-safe.

## Persistence

Version 3 adds WaterSortie to the executor save. Unversioned legacy empty trips and explicit Version2 remain supported; an invented explicit Version1 or mismatched extended phase is rejected. Source/fire application intent is deliberately not recreated on reload. Load only reads; first manager-owned Tick stops the stale path, clears the finite intent and releases to existing return recovery. Native inventory and independently saved return assignment remain authoritative. No water is inferred, consumed or refunded during Load, and no canceled fire application resumes. An empty pre-pickup assignment is inert.

## Tests and exact limits

- Full Release native suite: **1,778 passed, 0 failed/skipped**. Focused borrowed/transform/shared-water suite: **66 passed**. `/tmp/wildfire-folktails-connected-full-native.log`, `...connected-focused.log`.
- Actual SerializedWorldFactory.SaveEntity and BehaviorManager Save/Load preserve each water phase while first owned Tick cancels intent without source, equipment, simulator or navigation services. Supplied component caches and existing walk driver seam establish the lifecycle boundary; these are not full actor/game saves or positive Unity liveness.
- Native inventory cancellation preserves Water1 and independent recovery. Deliberately clearing that binding on cancellation produced a failing native regression, then restored source passed: `/tmp/wildfire-folktails-cancel-binding-red.log`.
- Preparation tests preserve a current foreign native executor, keep unassigned recovery inert, retain ordered arbitration and reject unsupported roots before poisoning/mutation. The actual manager enumerator IL grounds the preparation boundary; positive live BindReturn remains part of controller acceptance.
- Phase tests reject skipped physical arrivals and duplicate application. Command tests preserve exact identities/coordinate systems and reject malformed/diagnostics-only mutation. CLI transport is **8 passed** using isolated inbox/outbox files, not game execution. The first two new finite commands failed before CLI admission was added; `/tmp/wildfire-folktails-command-{red,green,final}.log`.

No engine, game, deployment or desktop action occurred for this source tranche. Lower native conversion/producer tests do not prove this concrete actor's positive normal-Runtime path. An actual tools-disabled Claude review completed against exact source `c8dfd88` in one request (407 seconds, exit 0). It identified fixed-destination deletion as a recovery limitation. Cargo and its assignment intentionally survive failed return attempts; clearing the assignment would erase provenance without recovering the water. Native arbitration releases unsuccessful recovery attempts so ordinary work can continue. Destination reselection remains unfinished. Raw review and disposition are retained at `/tmp/wildfire-connected-folktails-claude-review/`.

## First controller route

Use the sole controller, packaged exact source and a disposable populated Folktails world with ordinary employed adults, a small reachable clean riverbank, a nearby safe fire approach and an enabled same-district Water return inventory. Enable both existing QA mutations and `--wildfire-enable-borrowed-duty`. Keep real source geometry and native validators; do not inject Water1, fake Ready or manually replace the running executor.

```text
bun scripts/invoke-timberborn-command.ts qa-borrowed-duty-source <input-x> <input-y> <input-z> <shore-x> <shore-y> <shore-z>
bun scripts/invoke-timberborn-command.ts qa-borrowed-duty-water <donor-guid> <created-source-guid> <shore-x> <shore-y> <shore-z> <fire-cell> <approach-x> <approach-y> <approach-z> <return-owner-guid> <inventory-component>
bun scripts/invoke-timberborn-command.ts qa-borrowed-duty-status
bun scripts/invoke-timberborn-command.ts qa-borrowed-duty-cancel
```

Input coordinates use native z height; shore/approach use Unity y height. Factory admits exact cardinal bank at input height or one above; the created-source response exposes its actual Guid. Return ComponentName must identify exactly one native Inventory. Status exposes manager ownership, phase, native Water amount, current source Guid, finite intent and retained-return assignment, including idle actors with retained water. Phase alone is not conservation evidence.

Prove an actual workplace acceptance → physical shore arrival → real removal/settled credit → Water1 → physical fire approach → normal Runtime/GPU application and native consumption once → ordinary work. Record source buffers/column state and native inventory alongside actual field readback. Then cancel after pickup and physically deposit through return recovery. Save/reload while waiting, loaded and awaiting application; first owned recovery must not resume the fire or duplicate credit. Full/missing return storage, unsafe/disconnected/wet shore, extinguished target and queue-full controls retain truthful resources. Test actual native parallel water scheduling: earlier finite synchronous CPU columns do not establish it.

The integrated [native QA session service](native-session-control.md) supplies qa-game-speed 0/1 and asynchronous qa-save-copy/qa-save-status through the same transport. Its successful status, not file submission or actor phase, is the save completion evidence. No alternative control path is introduced.

Integrated source `c43c6c1`, including native session controls and the separately reviewed [critical-needs yield fix](borrowed-water-critical-needs.md), passes **1,808 native Release tests**, zero failures/skips. CLI transport passes eight tests; 375 generated blueprints match. Evidence: `/tmp/wildfire-connected-session-integrated-native.log`. Claude did not review the later needs or session-control changes. These checks do not replace physical trip and full game-save acceptance.
