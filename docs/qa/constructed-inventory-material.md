# Constructed inventory material admission

Source checkpoint `b0a633a`. Full native suite: **1,297 passed**, zero failures/skips. No engine, game, deployment or production world publication.

Owned initial formation and complete restored desired projections admit Structure/Stockpile bodies with original Stockpile, SimpleOutput and Manufactory declarations. The existing declaration capture owns the small shared material-support check used by original-evidence validation, physical compilation and composition diagnostics. Each physical row must retain the exact declared role and Inventory.ComponentName. Unsupported original roles reject even when empty, and supported constructed declarations cannot silently omit their physical row. The existing single natural GoodStack representation and its justified dormant-empty omission remain; no new natural stock effect, recovered pile or Warden inventory route is enabled.

Material composition includes one StoredGood part for each distinct positive GoodId across all admitted inventories. Exact physical rows remain separate, including disabled/reserved goods. Explicit accounting selections still name each exact declaration, and same-good selected quantities add with checked arithmetic. Construction costs, native resource/catalog accounting choices, the existing scalar material resolver and owner-level consequence budget are unchanged. This checkpoint does not select a first-release fuel normalization policy or infer remaining GPU fuel from current inventory.

The new complete-session regression uses a Structure with SimpleOutput Log3 and Manufactory Log2 plus construction Log3. It proves:

- Both exact rows compile to the body part plus a single Log material part; empty supported inventories add no material.
- Initial NativeResourceAmounts accounting is the existing `(3 construction + 5 selected stock) × 12 = 96`. OWNED4 stores both original declarations.
- Complete restore with current native observations Log1/1 retains capacity96 and the exact original encoded snapshot. It rebuilds the desired projection without consuming any stock or deriving a new body budget from remaining goods.
- A single exact-origin damage4 consequence removes the two remaining units through the existing guarded storage planner, once across both names. Native-effect adapter callbacks cannot save, nest resource mutation or capture the world during that transaction.
- A coherent current rename (both current declaration and physical row changed together) rejects against original OWNED4 evidence before allocation. Missing evidence, missing physical rows, foreign names, unsupported empty declarations and unknown positive goods also reject before allocation without poisoning a read-only failure.
- Same-good checked accounting overflow rejects. Unknown, duplicate-role/name and actual native alias topology regressions remain green in the full suite.

Session tests use immutable supplied native observations and a snapshot-capable managed simulator; consequence calls use a synthetic exact-declaration adapter. They are an integration proof of formation, restore, persistence, planning and guard composition, **not** actual Unity factory formation or native role consumption in a running world. Previously proved native role, reservation and inventory callback behavior remain covered by the installed-assembly suite. The source-only two-inventory engine probe at `/tmp/wildfire-two-inventory-engine-preparation` is preserved at its earlier rejection checkpoint; a separately pinned accepted-admission variant is still needed before any engine claim.

No native role enum or persistence schema changes. `UnsupportedInventoryRole` is only an appended transient composition diagnostic. Actual multi-inventory component ownership through the full provider, factory-backed formation/complete restore, final native callback rereads and live consumption remain controller validation gates.
