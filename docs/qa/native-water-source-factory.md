# Native water source factory and positive restore

Five CPU Unity Play-mode probes passed on immutable `2b28d8a86c263d2db0361966759ec51c34e55c56`. These exercise actual native creation and finite entity restoration. The ownership feature remains unbound in shipping gameplay.

| Proof | Actual result |
| --- | --- |
| A: new source | Native Bindito/template factory, EntityService and BlockObjectFactory create a live finished source; native adapter OnEnable activates its cache. Explicit post-creation arming succeeds with zero buffers. |
| A: credit/save | A **supplied completed removal result** credits 0.2 clean volume through the actual scheduled native service. Native EntitySaver writes the exact owner, coordinate and untainted positive buffer. |
| A: competitor/delete | A second native-created source shares the input coordinate and permanently taints the first. Native competitor deletion removes its registration without rearming the original. Subsequent save retains taint. |
| B: positive restore | A fresh graph parses A's exact saved bytes into an explicitly finite SerializedWorld. Actual WorldEntitiesLoader, EntitiesLoader, native block/terrain batch and PostLoad restore the same Guid, positive buffer, Ready/Armed state and exactly one input registration. |
| B: first cycle/resave | An empty actual settled scheduler cycle preserves clean 0.2/dirty 0 without new arming, receipt, refill or debit. Native loading issues are empty; native resave is byte-identical to the input. |

Both use the actual native adapter callback and normal Play-mode domain reload; no manual Awake/OnEnable, seeded instantiated-entity list or postload repair. The source blueprint is an authored nonvisual fixed input with installed Ground/no-entrance geometry. All nine native Game placement validators remain, using actual default terrain/column services and ceiling 4. The fixture's default GameInitializer state is false, and irrelevant absent-spec collaborators are explicitly unprovided. This is not an installed intake prefab or full game boot.

## Evidence and cleanup

| Run | Artifacts | Terminal / result |
| --- | --- | --- |
| A accepted | `/tmp/wildfire-natural-source-a-provider-corrected-run/` | 55364, PID 72913, exit 0; deferred destruction 8/8 after an actual player-loop frame; zero cleanup errors and Unity exceptions. |
| B accepted | `/tmp/wildfire-natural-source-b-engine-run/` | 81639, PID 73571, exit 0; native deletion 1, deferred destruction 5/5 after an actual frame; zero cleanup errors and Unity exceptions. |

Each directory preserves exact command, source/input manifests, result, native save, full logs and process outcome. All 19 A and 24 B prepared inputs matched after execution. CPU `-batchmode -nographics` uses licensed Editor **6000.3.6f1**, while installed game assemblies report **6000.5.5**. No GPU, game, Steam, deployment or desktop interaction occurred. No Persistent allocator notice appeared; unrelated shutdown thread/debugger notices remain in the logs and are not attributed to production.

Exact shared hashes:

- Native DLL: `066b261e8e5c86f18c3768e22a9c8c81fc5bf3fd75069f6588dd239bde82201a`
- Core DLL: `398d11c434bd822e69b7d60d83637ae583b05a572e30089b66e8e040ab8fe781`
- Native factory fixture: `c8b5060ac301c8fef98850cd6ec3c639bcc541115e1546c38178744570fdb0b7`
- A positive save / B exact input: `e2c5b5bb4a93ee78dd83c76c03856a5ad5a71aa6d06e9c741a7758f2b387b00b`

The saved owner is `c7b5187d-3869-453d-8471-d4cbd4ba1818`, template `Fixture.FixedWaterSource.A`, input `(3,1,4)`, ownership version 1, untainted, clean 0.2/dirty 0.

## Preserved failed fixture attempts

A's first attempt (`/tmp/wildfire-natural-source-a-engine-run/`, terminal 29740, exit 1) failed before entity creation because its reflection helper searched only already-loaded assemblies. Independently, immediate Play entry outran native delayed Editor Search index creation. Exact installed Editor IL and the startup correction are under `/tmp/wildfire-source-a-editor-search-review/`. The corrected runner waits for actual native startup and index creation; it neither changes native flags nor filters exceptions.

The second attempt (`/tmp/wildfire-natural-source-a-corrected-run/`, terminal 92029, exit 1) passed startup/resolution, then exposed a fixture provider reading the container before native eager construction returned it. The correction uses actual Bindito constructor-injected IContainer and preserves eager/scoping behavior; managed counterfactuals are under `/tmp/wildfire-natural-source-a-provider-injection-fix/`. Both failures and cleanup remain intact. Production DLLs were unchanged; the third A attempt and first B attempt passed.

## Actual removal and fresh restore follow-up

A separate **C** run replaced A's supplied receipt path with actual native demand, removal task and scheduled credit. `WaterInput.DemandCleanWaterAmount` uses the exact factory's native WaterService/WaterChangeService. Actual scheduler cycles publish demand and credit the real `UpdateWaterChangesTask.Run` result; no authored receipt, direct input-volume write or manual Changes.Tick remains. Task completion is synchronous over exclusively supplied finite native columns, between settled native scheduler cycles.

The installed one-Water quantum is 0.2. First native receipt/buffer was **0.199999988**, strictly insufficient; a second full request returned another **0.199999988**, accumulating **0.399999976**. Column depth decreased from 1 to 0.6 within the measured conservation tolerance. Actual empty tasks clear receipts and subsequent scheduler cycles do not replay credit. Native save retains the exact untainted positive buffer. Genuine empty-cycle competitor taint, deletion/unregistration and refusal to rearm also pass.

**B2** reused byte-identical B1 source, Entry, binaries, factory helper and supplier. Only the input bytes/hash and output paths changed. It restored C's exact native-removal save through the same real native loader/batch/PostLoad chain. The first empty scheduler cycle preserves clean 0.399999976/dirty 0, Ready/Armed state and one registration without new arming, receipt, refill or debit. Native loading issues are empty. Resave is **byte-identical** to C's input.

| Run | Artifacts | Terminal / result |
| --- | --- | --- |
| C: actual removal | `/tmp/wildfire-natural-source-real-fluid-run/` | 76452, PID 74274, exit 0; all 22 prepared inputs unchanged; deferred destruction 8/8 after an actual frame, zero cleanup errors/Unity exceptions. |
| B2: actual-removal save restore | `/tmp/wildfire-natural-source-b2-engine-run/` | 72237, PID 74588, exit 0; all 24 inputs unchanged; native deletion 1, deferred destruction 5/5 after an actual frame, zero cleanup errors/Unity exceptions. |

C adds the reviewed finite WaterSimulatorSpec supplier to its external helper (SHA `bd487c3f82398833fbc3d852cbc4b989a87c68b933cc378e019a607a98224102`); production native/Core and passed Entry/scene remain unchanged. B2 uses B1's original helper. The C save/B2 input/resave SHA is `a28a9bd73560b1ac3895ee1a49138d8ea94937af1567cddeb6d4678461b2303a`, owner `9410c0a3-d696-4211-af3c-936363cfd82a`, same template/input `(3,1,4)`. Original A/B1 artifacts retain their separate supplied-0.2 provenance.

## Native input measurements and publication

A fifth successful CPU probe uses actual WaterSimulator-created terrain columns and actual ThreadSafeWaterMap publication. The same native factory input reads this concrete map. Real AddCleanWater(0.75), AddContaminatedWater(0.25) and native removal tasks produce these exact observations:

| Stage | Native depth | Input IsUnderwater | Input ContaminationPercentage |
| --- | --- | --- | --- |
| Dry | 0 | false | 0 |
| Clean | 0.75 | true | 0 |
| Mixed | 1 | true | 0.25 |
| Drained / empty no-replay | 0 | false | 0 |

Six actual synchronous change tasks obtain the simulator's same arrays through native GetArray only during real StartParallelTick. No water columns, receipts, publication data or access flags are assigned by the fixture. Completed task results remain invisible to the prior published map until the following native Tick; the dry cell above does not inherit lower liquid. Native input buffers receive clean 0.75/dirty 0.25 once. Scheduler slot/metric/order checks pass. This measures native predicates without choosing pump/pristine policy.

Accepted artifacts: `/tmp/wildfire-native-water-map-settled-run/`, terminal **46623**, PID **78924**, exit **0**; all **23** input hashes unchanged. Native map subscriptions dispose, five GameObjects are destroyed after an actual frame, cleanup errors and Unity exceptions are zero. Production native/Core and accepted Entry remain unchanged. Map helper SHA `acc381301791c3de5588efe75cbe6548ef8b07aec663f1daca4e58a2e125a900`; corrected measurement SHA `d83ed2713222396d856102281d5b0c24bc267aa12d266c56783353077e98141d`.

The first attempt remains at `/tmp/wildfire-native-water-map-run/` (terminal 74367, exit 1): QueueOneTask correctly rejected startup before the scheduler became settled. Native map creation and initial dry checks were reached, but no task ran. The sole correction adds an actual empty TickAll/FinishParallelTick cycle with unchanged-state assertions before any enqueue. Both attempts completed cleanup. Nested simulator sequential lifecycle and synchronous task completion are explicitly fixture-owned; full native parallel fluid transport, gameplay policy, worker access and cargo conversion remain unproved. No further save/restore is claimed by this measurement.

## Remaining boundary

C+B2 establish finite native removal → exact credit → save → fresh native entity restoration without replay. Supplied finite columns and synchronous task completion do not prove full river simulation or native parallel WaterSimulationTaskStarter scheduling. B1/B2 supply finite serialized data rather than a game archive. Cleanliness/underwater admission, movement/shoreline access, tainted reload, gameplay demand cadence, native bucket conversion and a completed firefighter trip remain separate. [Ownership source/native tests](native-water-ownership-component.md) and these probes do not activate a production binding.
