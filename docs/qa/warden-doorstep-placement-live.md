# Warden placement and inventory-routing live boundary

On 2026-09-07, the sole live controller built and deployed exact source `b21f118c47427886624b20c613e0e55602cfa771` using the normal Release deployment and all four bundles. Thirty-nine copied files matched the staged source; the generated installed manifest made 40 installed files. Native DLL SHA256: `5732158ec43deb4f02e5976e8252a76491244ed25a6a8f42b92a25b2b6660306`; Core DLL: `d4ca0a9015636141fb7711e1390549a4354f4c0f23dde3a96182100e342eadae`.

One Steam launch created PID 20542 with process-only `--wildfire-enable-qa-mutations`. Saved Steam options and other mod selections were preserved. The disposable Iron Teeth Cliffside Easy baseline loaded with 13 beavers. This remained the ordinary WF1 runtime; no owned-runtime activation was performed.

## Passed live boundary

The Warden tool produced a green preview and actual placement completed without the previous pivot or doorstep exception. Native station `7f7df8f2-00d6-43eb-93bf-74030fa53399`, origin `(29,31,4)`, default orientation, displayed its real workplace and finite-water panel. The initial zero values were expected: workers 0/1, station water 0/20, assigned wardens 0/1, equipment water 0/1.

`Placed Warden b21f118.timber` was saved before path/worker experiments, backed up, and hashed `e77410b6857a3dff4112c7984ee3e98ca090a4702250cfd6ee2ca3fa374674b8` (141099 bytes). The original healthy save remained unchanged. Nineteen ordinary path cells ultimately linked the station's actual entrance to District Center; the disconnected warning cleared. An ordinary update/readiness sample reported exposure available and all 13 beavers sampled.

## New primary failure

The subsequent normal-speed native update threw `NullReferenceException` in `DistrictInventoryPicker.InventoryIsTaking`, through `ClosestInventoryWithCapacity`, `CarrierInventoryFinder`, `EmptyingStarter`, `EmptyOutputWorkplaceBehavior`, and `WorkerRootBehavior`. The tick wrapper identified BeaverAdult Aradya (`426f6d58-2ee1-4eb1-9b7a-35ffeef8dea4`). Its actual error-save `Worker.Workplace` was District Center `fd6deb9f-d87d-423e-b9f0-be747fdd6da2`, not the Warden Station. This is a native inventory-routing failure after connecting the new building; the responsible missing contract still requires diagnosis. The same preserved log records Warden `2507fdb2-6c48-46f4-9491-a5b59edbba3a` entering Idle with "No safely reachable fire". Its error-save Worker.Workplace is the station Guid, proving native assignment of Veakuajay before the unrelated worker tick failed. No refill, suppression, return, or operational save/reload is claimed.

The first failure was preserved and the game's own Exit button used; PID 20542 terminated. No unchanged retry followed. Native error report `error-report-2026-09-07-23h26m42s.zip` is 248460 bytes, SHA256 `7c353fe6b4f3c7d2756b5070dc37fe0907998d3b9f25a2f02073a605725cfc32`.

## Evidence and controller details

Local evidence: `/tmp/wildfire-warden-doorstep-live/` contains deployment logs/fingerprints, selected and unselected station images, the preserved checkpoint, `Player-staffing-failure.log`, `staffing-primary-stack.txt`, `staffing-inventory-primary-exception.jpg`, native error ZIP and extracted error-save/entity/path facts. The first UI stack has no IL offset. The second exception stack in `Player-staffing-failure.log` identifies `InventoryIsTaking` at `[0x0000f]`; source-level cause and repair remain the diagnosis task.

Mouse control used installed `cliclick` with observed screenshot coordinates mapped from actual CoreGraphics PID/window bounds. CUA screenshots and keyboard worked; its coordinate hit-testing failure was not an authorization denial. Auxiliary native windows changed the screenshot union, so geometry was re-read. Physical Alt+Shift+Z used the previously verified PID-guarded CoreGraphics keyboard helper. All keys/buttons were released. The live-controller lock remains owned by `/root/baseline_audit`, idle with no game process, pending the reviewed inventory fix.
