# Initial body compilation checkpoint

Source: `b6cac6f`; typed detachment integration: `37b4f2c`. This implements internal transient compilation, not a first-release accounting policy or runtime initial-world activation.

The compiler requires exactly one captured Guid selection and an explicit choice for each named yield/inventory role. Actual, declared and excluded yield choices are distinct. Disabled native getter zero cannot supply Actual accounting. Inventory accounting may be explicitly excluded; all positive physical stock still contributes material. Multiple native inventory roles remain unsupported. Selections carry no arbitrary quantity, resource id, footprint, material key, identity token or historical state.

NativeResourceAmounts and CatalogBodyProfile are mandatory accounting choices. Constructed costs are always captured exactly, including meaningful empty costs; the selected catalog profile deliberately takes precedence only when explicitly requested. Natural bodies require selected native resource accounting through the existing calculator. Two same-spec bodies can have different selected quantities because registrations use per-owner descriptor overrides. Zero actual accounting remains zero even when the plant's catalog GPU material is nonzero; that is an explicit proof input, not an adopted plant-body policy.

Material comes from existing body factories and exact captured positive stock. Known profiles and supported family are required. Tree, Crop, Structure and Stockpile are accepted. Vegetation, Infrastructure, standalone GoodStack and Unknown currently reject the entire capture; they are not silently omitted. Full-colony eligibility remains a release gate under a separate native audit. Physical stockpile body damage remains Structure. All overlapping contributors retain registrations and projections, including hidden contributors.

Twelve focused compiler cases pass: actual3 versus declared5, actual zero, disabled getter handling, missing/foreign/duplicate roles and Guids, multiple named inputs, explicit constructed basis, meaningful empty native cost, physical inventory preservation despite accounting exclusion, unknown stock, multiple inventory roles, unsupported family and copied selections with overlapping contributors. Together with updated detachment tests: **29 passed**. Full native suite at this checkpoint: **1,074 passed, zero failures/skips**. Logs are `/tmp/wildfire-initial-compiler-baseline-focused.txt` and `/tmp/wildfire-initial-body-compilation-full.txt`.

The detachment prototype now asks `Registry.CreateBaselineRequest` for uncovered cells. Scripted-backend tests prove OpenSoil retains packed Terrain0 with Terrain material class, and that Badwater/lower-owner reveal does not replay profile default wetness or reset current soil contamination. Those tests execute actual coordinator/registry authority, not GPU fire evolution.

The subsequent [initial session preparation](owned-initial-session-proof.md) implements the approved staged constructor and deterministic fixed environment projection. Its runtime adoption still requires the separate coverage and engine proof gates; its plan must not accept arbitrary initial field arrays. The session will hold the shared read exclusion through capture, selection, initial snapshot assembly, new backend construction and final complete capture equality, disposing an unpublished backend on failure. Root's shared body SameReadings and guarded restore reread are dependencies. No new-world fallback after failed restore is permitted. Rich desired-baseline restore composition remains a separate gate from this initializer; initial fields must never be replayed during later material reveal.
